{
  "pipeline": "hardcoded_secrets",
  "category": "security",
  "description": "Detects PHP variable assignments where the name suggests a secret and the value is a string literal.",
  "languages": ["php"],
  "graph": [
    {
      "match_pattern": "(assignment_expression left: (variable_name (name) @name (#match? @name \"(?i)(secret|password|api_key|token|credential|auth_key|private_key)\")) right: [(string) (encapsed_string)])"
    },
    {
      "flag": {
        "pattern": "hardcoded_secrets",
        "message": "Potential hardcoded secret in `{{name}}` — move to environment variable or secrets manager",
        "severity": "error"
      }
    }
  ]
}
