{
  "pipeline": "go_path_traversal",
  "category": "security",
  "description": "Detects path traversal risks in Go: filepath.Join and os.Open/Create/OpenFile with non-literal arguments. JSON version matches all selector_expression calls (cannot filter by package/method name without #match? predicate support). Precision reduced per D-07.",
  "languages": ["go"],
  "graph": [
    {
      "match_pattern": "(call_expression function: (selector_expression operand: (identifier) @pkg field: (field_identifier) @method)) @call"
    },
    {
      "flag": {
        "pattern": "unvalidated_path_join",
        "message": "Selector call expression detected -- verify filepath.Join or os.Open does not use unvalidated user input as path component",
        "severity": "warning"
      }
    }
  ]
}
