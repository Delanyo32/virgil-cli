{
  "pipeline": "type_assertions",
  "category": "code-quality",
  "description": "Detects `as` type assertions which override the type checker. Simplified from Rust: flags all as_expression nodes. Cannot distinguish `as const`, `as unknown` (safe casts), double assertions, or test file context. Language scoped to typescript only.",
  "languages": [
    "typescript"
  ],
  "graph": [
    {
      "match_pattern": "(as_expression) @assertion"
    },
    {
      "flag": {
        "pattern": "type_assertion",
        "message": "`as` type assertion detected -- overrides the type checker; ensure this is intentional and safe",
        "severity": "info"
      }
    }
  ]
}
