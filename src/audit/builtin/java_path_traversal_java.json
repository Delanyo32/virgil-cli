{
  "pipeline": "java_path_traversal",
  "category": "security",
  "description": "Detects path traversal risks in Java: new File() with concatenated paths, Paths.get() with dynamic arguments. JSON version matches all object_creation_expression nodes (cannot filter by type name or argument content without #match? predicate support). Precision reduced per D-07.",
  "languages": [
    "java"
  ],
  "graph": [
    {
      "match_pattern": "(object_creation_expression type: (type_identifier) @type arguments: (argument_list) @args) @creation"
    },
    {
      "flag": {
        "pattern": "unvalidated_path_operation",
        "message": "Object creation detected -- verify this is not new File() or new ProcessBuilder() with unvalidated user-controlled path arguments",
        "severity": "warning"
      }
    }
  ]
}
