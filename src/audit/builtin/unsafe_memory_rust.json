{
  "pipeline": "unsafe_memory",
  "category": "security",
  "description": "Detects unsafe memory operations in Rust: raw pointer dereference, pointer arithmetic, transmute in unsafe blocks. Simplified from Rust: flags all unsafe blocks rather than specific sub-operations (transmute, pointer arithmetic, raw deref). Precision reduced -- all unsafe blocks flagged regardless of content.",
  "languages": ["rust"],
  "graph": [
    {
      "match_pattern": "(unsafe_block (block) @unsafe_body) @unsafe_blk"
    },
    {
      "flag": {
        "pattern": "unsafe_memory_operation",
        "message": "Unsafe block detected -- verify no transmute, pointer arithmetic, or raw dereference of untrusted data",
        "severity": "warning"
      }
    }
  ]
}
