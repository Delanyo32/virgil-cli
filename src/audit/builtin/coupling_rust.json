{
  "pipeline": "coupling",
  "category": "code-quality",
  "description": "Detects excessive imports, parameter overload, and low-cohesion methods in Rust. Simplified from Rust: counts use declarations per file as a coupling proxy. Does not compute cross-file dependency fan-out, parameter overload per function, or low-cohesion method detection.",
  "languages": ["rust"],
  "graph": [
    {
      "match_pattern": "(use_declaration) @import"
    },
    {
      "flag": {
        "pattern": "high_coupling",
        "message": "use declaration detected -- monitor total import count; high coupling indicates a module doing too much",
        "severity": "info"
      }
    }
  ]
}
