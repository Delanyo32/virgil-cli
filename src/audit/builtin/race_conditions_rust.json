{
  "pipeline": "race_conditions",
  "category": "security",
  "description": "Detects race condition risks in Rust: static mut variable declarations create data race potential. Pure tree-sitter detection of static_item with mutable_specifier. Languages field scoped to rust only to avoid conflict with Go race_conditions pipeline (same pipeline name).",
  "languages": [
    "rust"
  ],
  "graph": [
    {
      "match_pattern": "(static_item (mutable_specifier) @mut_spec) @static_mut_item"
    },
    {
      "flag": {
        "pattern": "static_mut",
        "message": "static mut variable creates data race potential -- consider AtomicXxx or Mutex",
        "severity": "error"
      }
    }
  ]
}
