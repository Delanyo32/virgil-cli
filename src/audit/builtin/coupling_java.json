{
  "pipeline": "coupling",
  "category": "code-quality",
  "description": "Detects high coupling in Java files via import declarations. Simplified from Rust: flags all import_declaration nodes as coupling indicators. Cannot count total imports against the 15-import threshold, detect parameter overload (>=5 params), or check low-cohesion methods.",
  "languages": [
    "java"
  ],
  "graph": [
    {
      "match_pattern": "(import_declaration) @import_stmt"
    },
    {
      "flag": {
        "pattern": "excessive_imports",
        "message": "import declaration detected -- monitor total import count; high coupling (>=15 imports) indicates a class doing too much",
        "severity": "info"
      }
    }
  ]
}
