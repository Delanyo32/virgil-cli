{
  "pipeline": "loose_equality",
  "category": "code-quality",
  "description": "Detects `==` and `!=` operators in JavaScript which perform type coercion. Simplified from Rust: flags all binary_expression nodes without filtering out typeof comparisons or null checks. Prefer `===` and `!==` for strict comparison.",
  "languages": [
    "javascript"
  ],
  "graph": [
    {
      "match_pattern": "[(binary_expression operator: \"==\" @op) (binary_expression operator: \"!=\" @op)] @expr"
    },
    {
      "flag": {
        "pattern": "loose_equality",
        "message": "Loose equality operator detected -- use `===` or `!==` for strict comparison to avoid type coercion",
        "severity": "warning"
      }
    }
  ]
}
