{
  "pipeline": "unconstrained_generics",
  "category": "code-quality",
  "description": "Detects generic type parameters without `extends` constraints in function/method signatures. Simplified from Rust: flags all type_parameter nodes. Cannot check for constraint field, default type value, or limit to function/method context (class-level generics also flagged). Language scoped to typescript only.",
  "languages": [
    "typescript"
  ],
  "graph": [
    {
      "match_pattern": "(type_parameter name: (type_identifier) @name) @param"
    },
    {
      "flag": {
        "pattern": "unconstrained_generic",
        "message": "Generic type parameter detected without `extends` constraint -- callers can pass any type; consider adding a constraint",
        "severity": "info"
      }
    }
  ]
}
