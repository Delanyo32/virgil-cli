{
  "pipeline": "hardcoded_secrets",
  "category": "security",
  "description": "Detects Python assignments where the variable name suggests a secret and the value is a string literal.",
  "languages": ["python"],
  "graph": [
    {
      "match_pattern": "(assignment left: (identifier) @name (#match? @name \"(?i)(secret|password|api_key|token|credential|auth_key|private_key)\") right: (string))"
    },
    {
      "flag": {
        "pattern": "hardcoded_secrets",
        "message": "Potential hardcoded secret in `{{name}}` — move to environment variable or secrets manager",
        "severity": "error"
      }
    }
  ]
}
