{
  "pipeline": "unchecked_malloc",
  "category": "code-quality",
  "description": "Detects malloc/calloc/realloc calls without null-check on the returned pointer. Simplified from Rust: flags all allocation call_expression nodes without verifying null check follows. Languages scoped to c only.",
  "languages": ["c"],
  "graph": [
    {
      "match_pattern": "(call_expression function: (identifier) @fn_name arguments: (argument_list) @args) @call"
    },
    {
      "flag": {
        "pattern": "unchecked_allocation",
        "message": "Memory allocation call detected -- verify malloc(), calloc(), realloc(), aligned_alloc(), and mmap() return values are checked for NULL before use to prevent null pointer dereferences.",
        "severity": "error"
      }
    }
  ]
}
