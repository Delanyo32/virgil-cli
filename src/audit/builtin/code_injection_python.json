{
  "pipeline": "code_injection",
  "category": "security",
  "description": "Detects code injection risks in Python: eval(), exec(), compile() with dynamic arguments. Simplified from Rust: flags all direct identifier function calls. Cannot filter to only eval/exec/compile or check for non-literal arguments without #match? predicate support.",
  "languages": ["python"],
  "graph": [
    {
      "match_pattern": "(call function: (identifier) @fn)"
    },
    {
      "flag": {
        "pattern": "code_injection_call",
        "message": "Direct function call detected -- verify this is not eval(), exec(), or compile() with a dynamic argument",
        "severity": "error"
      }
    }
  ]
}
