{
  "pipeline": "dead_code",
  "category": "code-quality",
  "description": "Detects potentially unused private methods and unused imports in Java. Simplified from Rust: flags method and function symbols as candidates. Cannot detect unused private methods (no cross-file reference counting), unused imports, or unreachable code after return/break/continue/throw.",
  "languages": ["java"],
  "graph": [
    {
      "select": "symbol",
      "where": {"kind": ["method", "function"]},
      "exclude": {"is_test_file": true}
    },
    {
      "flag": {
        "pattern": "potentially_dead_export",
        "message": "method/function detected -- verify it is actually referenced (dead code detection simplified: cross-file reference counting not available in JSON DSL)",
        "severity": "info"
      }
    }
  ]
}
