{
  "pipeline": "init_abuse",
  "category": "code-quality",
  "description": "Detects side effects in Go init() functions: database opens, HTTP calls, file I/O, and other startup-time anti-patterns. Simplified from Rust: flags all function declarations named 'init'. Cannot filter for specific side-effect call patterns inside the init body.",
  "languages": [
    "go"
  ],
  "graph": [
    {
      "match_pattern": "(function_declaration name: (identifier) @name body: (block) @body) @fn"
    },
    {
      "flag": {
        "pattern": "init_function_abuse",
        "message": "init() function detected -- verify it does not contain side effects (DB opens, HTTP calls, file I/O) that should be explicit initialization",
        "severity": "info"
      }
    }
  ]
}
