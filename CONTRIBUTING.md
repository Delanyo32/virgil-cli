# Contributing to [Project Name]

## Getting Started
1. Fork the repo
2. Clone your fork: `git clone https://github.com/YOUR-USERNAME/repo.git`
3. Create a feature branch: `git checkout -b feature/your-feature`
4. Install dependencies: `[your install command]`

## Development Workflow
1. **Open an issue first** — describe what you want to change
2. Wait for maintainer feedback before starting work
3. Make your changes in a feature branch
4. Write/update tests
5. Submit a PR referencing the issue

## Code Style
- [Your formatting/linting rules]
- Run `[lint command]` before submitting

## Commit Messages
Use conventional commits: `feat:`, `fix:`, `docs:`, `refactor:`

## Pull Request Process
1. PRs require at least 1 approval
2. All CI checks must pass
3. Link the related issue using `Closes #issue-number`