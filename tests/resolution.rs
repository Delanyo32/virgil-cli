//! Issue #18.2 — synthetic-factbase tests for the references resolver.
//! Each scenario gets its own module file under `tests/resolution/`.
//! One test binary; one named test function per scenario file.

mod resolution {
    pub mod shadowing;
}
