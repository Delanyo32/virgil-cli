# benchmarks/python/technical-debt — Python full extractor (#18.3)
# Issue #18.1: class-body assignments now emit Field symbols → binding +
# references counts bump up; field_type joins to symbol.
occurrence_total = 10452
scope_total = 835
binding_total = 3687
references_total = 26178
references_resolved = 21554
references_unresolved = 4624
# Benchmark has only untyped class-body assignments; field symbols exist
# (16) but field_type emits no row without an annotation, so the JOIN is
# 0 here. The fix in #18.1 enables the JOIN when typed fields appear.
field_type_joinable_with_symbol = 0
