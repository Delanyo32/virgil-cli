---
phase: 04-security-per-language-scalability-migration
plan: 08
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/audit/builtin/command_injection_csharp.json
  - src/audit/builtin/weak_cryptography_csharp.json
  - src/audit/builtin/insecure_deserialization_csharp.json
  - src/audit/builtin/csharp_path_traversal_csharp.json
  - src/audit/builtin/csharp_race_conditions_csharp.json
  - src/audit/builtin/reflection_unsafe_csharp.json
  - src/audit/builtin/memory_leak_indicators_csharp.json
  - src/audit/pipelines/csharp/mod.rs
  - tests/audit_json_integration.rs
autonomous: true
requirements: [SEC-01, SEC-02, SCAL-02, SCAL-03, TEST-01, TEST-02]

must_haves:
  truths:
    - "C# security pipelines produce findings via JSON for command_injection, weak_cryptography, insecure_deserialization, csharp_path_traversal, csharp_race_conditions, reflection_unsafe"
    - "C# memory_leak_indicators produces findings via JSON"
    - "sql_injection, xxe, csharp_ssrf remain as Rust pipelines (permanent exceptions)"
    - "cargo test passes with zero failures"
  artifacts:
    - path: "src/audit/builtin/command_injection_csharp.json"
      provides: "JSON security pipeline for C# command injection"
      contains: "\"pipeline\": \"command_injection\""
    - path: "src/audit/builtin/memory_leak_indicators_csharp.json"
      provides: "JSON scalability pipeline for C# memory leak indicators"
      contains: "\"pipeline\": \"memory_leak_indicators\""
  key_links:
    - from: "src/audit/builtin/*_csharp.json"
      to: "src/audit/engine.rs"
      via: "include_dir auto-discovery + name-match suppression (ENG-01)"
      pattern: "json_pipeline_names.contains"
---

<objective>
Migrate 6 C# security pipelines and the memory_leak_indicators scalability pipeline from Rust to JSON. Leave sql_injection, xxe, and csharp_ssrf as permanent Rust exceptions.

Purpose: C# has 3 permanent exceptions (same as Java). The remaining pipelines are pure tree-sitter.
Output: 7 JSON files, 7 Rust .rs files deleted, integration tests.

**PERMANENT RUST EXCEPTIONS:**
- `sql_injection` -- requires FlowsTo/SanitizedBy graph predicates
- `xxe` -- requires taint through XML parser
- `csharp_ssrf` -- requires FlowsTo/SanitizedBy graph predicates
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From src/audit/pipelines/csharp/mod.rs security_pipelines():
- sql_injection -> KEEP
- command_injection -> "command_injection"
- weak_cryptography -> "weak_cryptography"
- insecure_deserialization -> "insecure_deserialization"
- csharp_path_traversal -> "csharp_path_traversal"
- xxe -> KEEP
- csharp_ssrf -> KEEP
- csharp_race_conditions -> "csharp_race_conditions"
- reflection_unsafe -> "reflection_unsafe"

All files use "languages": ["csharp"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 JSON pipeline files for C# security + scalability</name>
  <files>
    src/audit/builtin/command_injection_csharp.json,
    src/audit/builtin/weak_cryptography_csharp.json,
    src/audit/builtin/insecure_deserialization_csharp.json,
    src/audit/builtin/csharp_path_traversal_csharp.json,
    src/audit/builtin/csharp_race_conditions_csharp.json,
    src/audit/builtin/reflection_unsafe_csharp.json,
    src/audit/builtin/memory_leak_indicators_csharp.json
  </files>
  <read_first>
    src/audit/pipelines/csharp/command_injection.rs,
    src/audit/pipelines/csharp/weak_cryptography.rs,
    src/audit/pipelines/csharp/insecure_deserialization.rs,
    src/audit/pipelines/csharp/csharp_path_traversal.rs,
    src/audit/pipelines/csharp/csharp_race_conditions.rs,
    src/audit/pipelines/csharp/reflection_unsafe.rs,
    src/audit/pipelines/csharp/memory_leak_indicators.rs
  </read_first>
  <action>
Create 7 JSON files. All use "languages": ["csharp"]. Read each Rust source and translate.

1. **command_injection_csharp.json** -- pipeline: "command_injection". Rust detects Process.Start with dynamic args, cmd.exe calls. Pattern: "command_injection_call", severity: "error".

2. **weak_cryptography_csharp.json** -- pipeline: "weak_cryptography". Rust detects MD5/SHA1/DES instantiation by type name. Pattern: "weak_crypto_usage", severity: "warning".

3. **insecure_deserialization_csharp.json** -- pipeline: "insecure_deserialization". Rust detects BinaryFormatter.Deserialize, JsonConvert.DeserializeObject. Pattern: "insecure_deserialization", severity: "error".

4. **csharp_path_traversal_csharp.json** -- pipeline: "csharp_path_traversal". Rust detects Path.Combine/File.Open with dynamic component. Pattern: "path_traversal_risk", severity: "warning".

5. **csharp_race_conditions_csharp.json** -- pipeline: "csharp_race_conditions". Rust detects static non-Interlocked non-Volatile fields, thread-unsafe singleton patterns. Simplified: match static field declarations. Pattern: "thread_unsafe_field", severity: "warning".

6. **reflection_unsafe_csharp.json** -- pipeline: "reflection_unsafe". Rust detects Assembly.Load/Type.GetMethod/Invoke with dynamic strings. Pattern: "reflection_injection", severity: "warning".

7. **memory_leak_indicators_csharp.json** -- pipeline: "memory_leak_indicators", category: "scalability". Simplified per D-09: match object_creation_expression for known IDisposable types (SqlConnection, HttpClient, StreamReader, FileStream). Pattern: "potential_memory_leak", severity: "warning". Document: "Simplified: flags IDisposable type instantiations without verifying using block."

Run `cargo test`.
  </action>
  <verify>
    <automated>cargo test --test audit_json_integration 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 JSON files exist with _csharp suffix
    - Each has "languages": ["csharp"]
    - cargo test compiles without errors
  </acceptance_criteria>
  <done>7 C# JSON files created, cargo test passes</done>
</task>

<task type="auto">
  <name>Task 2: Delete C# Rust pipeline files + add integration tests</name>
  <files>
    src/audit/pipelines/csharp/mod.rs,
    tests/audit_json_integration.rs
  </files>
  <read_first>
    src/audit/pipelines/csharp/mod.rs,
    tests/audit_json_integration.rs
  </read_first>
  <action>
**Step 1: Delete 7 Rust files from csharp/:**
Delete: command_injection.rs, weak_cryptography.rs, insecure_deserialization.rs, csharp_path_traversal.rs, csharp_race_conditions.rs, reflection_unsafe.rs, memory_leak_indicators.rs

**DO NOT delete:** sql_injection.rs, xxe.rs, csharp_ssrf.rs (permanent exceptions)

**Step 2: Update csharp/mod.rs:**
Remove deleted pub mod lines. Keep sql_injection, xxe, csharp_ssrf.
Update security_pipelines() to return only permanent exceptions:
```rust
pub fn security_pipelines() -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![
        Box::new(sql_injection::SqlInjectionPipeline::new()?),
        Box::new(xxe::XxePipeline::new()?),
        Box::new(csharp_ssrf::CsharpSsrfPipeline::new()?),
    ])
}
```
Update scalability_pipelines() to return empty vec.

**Step 3: Add integration tests (14 tests: 7 positive + 7 negative).**
Use Language::CSharp and ".cs" files.

Key fixtures:
- command_injection positive: `using System.Diagnostics;\nclass A { void F(string cmd) { Process.Start("cmd.exe", cmd); } }`
- weak_cryptography positive: `using System.Security.Cryptography;\nclass A { void F() { MD5.Create(); } }`
- memory_leak positive: `using System.Data.SqlClient;\nclass A { void F() { var c = new SqlConnection("connstr"); } }`

Run `cargo test`.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 Rust files deleted from csharp/
    - sql_injection.rs, xxe.rs, csharp_ssrf.rs STILL EXIST
    - csharp/mod.rs security_pipelines() returns vec with only 3 permanent exceptions
    - cargo test passes with zero failures
  </acceptance_criteria>
  <done>7 C# Rust files deleted, mod.rs updated preserving 3 exceptions, integration tests added, cargo test passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files -> executor | Compile-time embedded JSON; no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | src/audit/builtin/*_csharp.json | accept | Same as T-04-01 |
</threat_model>

<verification>
1. `cargo test` passes
2. 3 permanent exceptions preserved
</verification>

<success_criteria>
- 7 JSON created, 7 Rust deleted, 3 exceptions preserved, tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-per-language-scalability-migration/04-08-SUMMARY.md`
</output>
