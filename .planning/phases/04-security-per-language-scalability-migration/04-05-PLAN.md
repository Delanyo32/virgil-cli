---
phase: 04-security-per-language-scalability-migration
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/audit/builtin/command_injection_java.json
  - src/audit/builtin/weak_cryptography_java.json
  - src/audit/builtin/insecure_deserialization_java.json
  - src/audit/builtin/java_path_traversal_java.json
  - src/audit/builtin/reflection_injection_java.json
  - src/audit/builtin/java_race_conditions_java.json
  - src/audit/builtin/memory_leak_indicators_java.json
  - src/audit/pipelines/java/mod.rs
  - tests/audit_json_integration.rs
autonomous: true
requirements: [SEC-01, SEC-02, SCAL-02, SCAL-03, TEST-01, TEST-02]

must_haves:
  truths:
    - "Java security pipelines produce findings via JSON for command_injection, weak_cryptography, insecure_deserialization, java_path_traversal, reflection_injection, java_race_conditions"
    - "Java memory_leak_indicators produces findings via JSON"
    - "sql_injection, xxe, java_ssrf remain as Rust pipelines (permanent exceptions)"
    - "cargo test passes with zero failures"
  artifacts:
    - path: "src/audit/builtin/command_injection_java.json"
      provides: "JSON security pipeline for Java command injection"
      contains: "\"pipeline\": \"command_injection\""
    - path: "src/audit/builtin/memory_leak_indicators_java.json"
      provides: "JSON scalability pipeline for Java memory leak indicators"
      contains: "\"pipeline\": \"memory_leak_indicators\""
  key_links:
    - from: "src/audit/builtin/*_java.json"
      to: "src/audit/engine.rs"
      via: "include_dir auto-discovery + name-match suppression (ENG-01)"
      pattern: "json_pipeline_names.contains"
---

<objective>
Migrate 6 Java security pipelines and the memory_leak_indicators scalability pipeline from Rust to JSON. Leave sql_injection, xxe, and java_ssrf as permanent Rust exceptions.

Purpose: Java has 3 permanent exceptions (most of any language group). The remaining 6 security pipelines are all pure tree-sitter.
Output: 7 JSON files, 7 Rust .rs files deleted, integration tests.

**PERMANENT RUST EXCEPTIONS:**
- `sql_injection` -- requires FlowsTo/SanitizedBy graph predicates
- `xxe` -- requires taint through XML parser
- `java_ssrf` -- requires FlowsTo/SanitizedBy graph predicates
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From src/audit/pipelines/java/mod.rs security_pipelines():
- sql_injection -> KEEP (permanent exception)
- command_injection -> "command_injection"
- weak_cryptography -> "weak_cryptography"
- insecure_deserialization -> "insecure_deserialization"
- java_path_traversal -> "java_path_traversal"
- xxe -> KEEP (permanent exception)
- java_ssrf -> KEEP (permanent exception)
- reflection_injection -> "reflection_injection"
- java_race_conditions -> "java_race_conditions"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 JSON pipeline files for Java security + scalability</name>
  <files>
    src/audit/builtin/command_injection_java.json,
    src/audit/builtin/weak_cryptography_java.json,
    src/audit/builtin/insecure_deserialization_java.json,
    src/audit/builtin/java_path_traversal_java.json,
    src/audit/builtin/reflection_injection_java.json,
    src/audit/builtin/java_race_conditions_java.json,
    src/audit/builtin/memory_leak_indicators_java.json
  </files>
  <read_first>
    src/audit/pipelines/java/command_injection.rs,
    src/audit/pipelines/java/weak_cryptography.rs,
    src/audit/pipelines/java/insecure_deserialization.rs,
    src/audit/pipelines/java/java_path_traversal.rs,
    src/audit/pipelines/java/reflection_injection.rs,
    src/audit/pipelines/java/java_race_conditions.rs,
    src/audit/pipelines/java/memory_leak_indicators.rs
  </read_first>
  <action>
Create 7 JSON files. All use "languages": ["java"]. Read each Rust source and translate patterns.

1. **command_injection_java.json** -- pipeline: "command_injection". Rust detects Runtime.exec/ProcessBuilder with dynamic args. Pattern: "command_injection_call", severity: "error".

2. **weak_cryptography_java.json** -- pipeline: "weak_cryptography". Rust detects MD5/SHA1/DES instantiation by name. Pattern: "weak_crypto_usage", severity: "warning".

3. **insecure_deserialization_java.json** -- pipeline: "insecure_deserialization". Rust detects ObjectInputStream.readObject(), XStream.fromXML. Pattern: "insecure_deserialization", severity: "error".

4. **java_path_traversal_java.json** -- pipeline: "java_path_traversal". Rust detects Paths.get/new File with dynamic component. Pattern: "unvalidated_path_operation", severity: "warning".

5. **reflection_injection_java.json** -- pipeline: "reflection_injection". Rust detects Class.forName/Method.invoke with dynamic string. Pattern: "reflection_injection", severity: "warning".

6. **java_race_conditions_java.json** -- pipeline: "java_race_conditions". Rust detects unsynchronized shared collection fields. Pattern: "thread_unsafe_collection", severity: "warning".

7. **memory_leak_indicators_java.json** -- pipeline: "memory_leak_indicators", category: "scalability". Simplified per D-09: match new expressions for common resource types (Connection, InputStream, OutputStream, etc.). Pattern: "potential_memory_leak", severity: "warning". Document: "Simplified: flags resource type instantiations without verifying try-with-resources closure."

Run `cargo test`.
  </action>
  <verify>
    <automated>cargo test --test audit_json_integration 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 JSON files exist with _java suffix
    - Each has "languages": ["java"]
    - cargo test compiles without errors
  </acceptance_criteria>
  <done>7 Java JSON files created, cargo test passes</done>
</task>

<task type="auto">
  <name>Task 2: Delete Java Rust pipeline files + add integration tests</name>
  <files>
    src/audit/pipelines/java/mod.rs,
    tests/audit_json_integration.rs
  </files>
  <read_first>
    src/audit/pipelines/java/mod.rs,
    tests/audit_json_integration.rs
  </read_first>
  <action>
**Step 1: Delete 7 Rust files from java/:**
Delete: command_injection.rs, weak_cryptography.rs, insecure_deserialization.rs, java_path_traversal.rs, reflection_injection.rs, java_race_conditions.rs, memory_leak_indicators.rs

**DO NOT delete:** sql_injection.rs, xxe.rs, java_ssrf.rs (permanent exceptions)

**Step 2: Update java/mod.rs:**
Remove pub mod lines for deleted modules. Keep sql_injection, xxe, java_ssrf.
Update security_pipelines() to return only permanent exceptions:
```rust
pub fn security_pipelines() -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![
        Box::new(sql_injection::SqlInjectionPipeline::new()?),
        Box::new(xxe::XxePipeline::new()?),
        Box::new(java_ssrf::JavaSsrfPipeline::new()?),
    ])
}
```
Update scalability_pipelines() to return empty vec.

**Step 3: Add integration tests (14 tests: 7 positive + 7 negative).**
Use Language::Java and ".java" files.

Key fixtures:
- command_injection positive: `class A { void f(String cmd) { Runtime.getRuntime().exec(cmd); } }`
- weak_cryptography positive: `import java.security.*;\nclass A { void f() { MessageDigest.getInstance("MD5"); } }`
- memory_leak_indicators positive: `import java.sql.*;\nclass A { void f() { Connection c = DriverManager.getConnection("url"); } }`

Run `cargo test`.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 Rust files deleted from java/
    - sql_injection.rs, xxe.rs, java_ssrf.rs STILL EXIST
    - java/mod.rs security_pipelines() returns vec with only 3 permanent exceptions
    - cargo test passes with zero failures
  </acceptance_criteria>
  <done>7 Java Rust files deleted, mod.rs updated preserving 3 exceptions, integration tests added, cargo test passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files -> executor | Compile-time embedded JSON; no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | src/audit/builtin/*_java.json | accept | Same as T-04-01 |
</threat_model>

<verification>
1. `cargo test` passes
2. sql_injection.rs, xxe.rs, java_ssrf.rs preserved
</verification>

<success_criteria>
- 7 JSON created, 7 Rust deleted, 3 permanent exceptions preserved, tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-per-language-scalability-migration/04-05-SUMMARY.md`
</output>
