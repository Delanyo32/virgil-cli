---
phase: 04-security-per-language-scalability-migration
plan: 09
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/audit/builtin/unsafe_include_php.json
  - src/audit/builtin/unescaped_output_php.json
  - src/audit/builtin/command_injection_php.json
  - src/audit/builtin/insecure_deserialization_php.json
  - src/audit/builtin/type_juggling_php.json
  - src/audit/builtin/session_auth_php.json
  - src/audit/builtin/memory_leak_indicators_php.json
  - src/audit/pipelines/php/mod.rs
  - tests/audit_json_integration.rs
autonomous: true
requirements: [SEC-01, SEC-02, SCAL-02, SCAL-03, TEST-01, TEST-02]

must_haves:
  truths:
    - "PHP security pipelines produce findings via JSON for unsafe_include, unescaped_output, command_injection, insecure_deserialization, type_juggling, session_auth"
    - "PHP memory_leak_indicators produces findings via JSON"
    - "sql_injection and ssrf remain as Rust pipelines (permanent exceptions)"
    - "cargo test passes with zero failures"
  artifacts:
    - path: "src/audit/builtin/command_injection_php.json"
      provides: "JSON security pipeline for PHP command injection"
      contains: "\"pipeline\": \"command_injection\""
    - path: "src/audit/builtin/unsafe_include_php.json"
      provides: "JSON security pipeline for PHP unsafe include"
      contains: "\"pipeline\": \"unsafe_include\""
    - path: "src/audit/builtin/memory_leak_indicators_php.json"
      provides: "JSON scalability pipeline for PHP memory leak indicators"
      contains: "\"pipeline\": \"memory_leak_indicators\""
  key_links:
    - from: "src/audit/builtin/*_php.json"
      to: "src/audit/engine.rs"
      via: "include_dir auto-discovery + name-match suppression (ENG-01)"
      pattern: "json_pipeline_names.contains"
---

<objective>
Migrate 6 PHP security pipelines and the memory_leak_indicators scalability pipeline from Rust to JSON. Leave sql_injection and ssrf as permanent Rust exceptions.

Purpose: PHP has unique pipeline names (unsafe_include, unescaped_output, type_juggling, session_auth) not found in other languages. All are pure tree-sitter.
Output: 7 JSON files, 7 Rust .rs files deleted, integration tests.

**PERMANENT RUST EXCEPTIONS:**
- `sql_injection` -- requires FlowsTo/SanitizedBy graph predicates
- `ssrf` -- requires FlowsTo/SanitizedBy graph predicates
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From src/audit/pipelines/php/mod.rs security_pipelines():
- sql_injection -> KEEP
- unsafe_include -> "unsafe_include"
- unescaped_output -> "unescaped_output"
- command_injection -> "command_injection"
- insecure_deserialization -> "insecure_deserialization"
- type_juggling -> "type_juggling"
- ssrf -> KEEP
- session_auth -> "session_auth"

All files use "languages": ["php"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 JSON pipeline files for PHP security + scalability</name>
  <files>
    src/audit/builtin/unsafe_include_php.json,
    src/audit/builtin/unescaped_output_php.json,
    src/audit/builtin/command_injection_php.json,
    src/audit/builtin/insecure_deserialization_php.json,
    src/audit/builtin/type_juggling_php.json,
    src/audit/builtin/session_auth_php.json,
    src/audit/builtin/memory_leak_indicators_php.json
  </files>
  <read_first>
    src/audit/pipelines/php/unsafe_include.rs,
    src/audit/pipelines/php/unescaped_output.rs,
    src/audit/pipelines/php/command_injection.rs,
    src/audit/pipelines/php/insecure_deserialization.rs,
    src/audit/pipelines/php/type_juggling.rs,
    src/audit/pipelines/php/session_auth.rs,
    src/audit/pipelines/php/memory_leak_indicators.rs
  </read_first>
  <action>
Create 7 JSON files. All use "languages": ["php"]. Read each Rust source and translate.

1. **unsafe_include_php.json** -- pipeline: "unsafe_include". Rust detects include/require/include_once/require_once with dynamic path. Match include_expression/require_expression nodes. Pattern: "unsafe_include", severity: "error".

2. **unescaped_output_php.json** -- pipeline: "unescaped_output". Rust detects echo/print with $_GET/$_POST without htmlspecialchars. Simplified: match echo_statement nodes broadly. Pattern: "unescaped_output", severity: "warning". Document precision loss.

3. **command_injection_php.json** -- pipeline: "command_injection". Rust detects shell_exec/exec/system/passthru with dynamic arg. Pattern: "command_injection_call", severity: "error".

4. **insecure_deserialization_php.json** -- pipeline: "insecure_deserialization". Rust detects unserialize() with non-literal arg. Pattern: "insecure_deserialization", severity: "error".

5. **type_juggling_php.json** -- pipeline: "type_juggling". Rust detects loose comparison (==) on security-sensitive values. Match binary_expression with == operator. Pattern: "loose_comparison", severity: "warning".

6. **session_auth_php.json** -- pipeline: "session_auth". Rust detects session_start() missing, session_regenerate_id() missing after auth. Simplified: match function_call_expression nodes broadly. Pattern: "session_management", severity: "warning". Document precision loss.

7. **memory_leak_indicators_php.json** -- pipeline: "memory_leak_indicators", category: "scalability". Simplified per D-09: match fopen/curl_init/mysql_connect calls + array subscript assignment in loops. Pattern: "potential_memory_leak", severity: "warning".

Run `cargo test`.
  </action>
  <verify>
    <automated>cargo test --test audit_json_integration 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 JSON files exist with _php suffix
    - Each has "languages": ["php"]
    - cargo test compiles without errors
  </acceptance_criteria>
  <done>7 PHP JSON files created, cargo test passes</done>
</task>

<task type="auto">
  <name>Task 2: Delete PHP Rust pipeline files + add integration tests</name>
  <files>
    src/audit/pipelines/php/mod.rs,
    tests/audit_json_integration.rs
  </files>
  <read_first>
    src/audit/pipelines/php/mod.rs,
    tests/audit_json_integration.rs
  </read_first>
  <action>
**Step 1: Delete 7 Rust files from php/:**
Delete: unsafe_include.rs, unescaped_output.rs, command_injection.rs, insecure_deserialization.rs, type_juggling.rs, session_auth.rs, memory_leak_indicators.rs

**DO NOT delete:** sql_injection.rs, ssrf.rs (permanent exceptions)

**Step 2: Update php/mod.rs:**
Remove deleted pub mod lines. Keep sql_injection and ssrf.
Update security_pipelines() to return only permanent exceptions:
```rust
pub fn security_pipelines() -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![
        Box::new(sql_injection::SqlInjectionPipeline::new()?),
        Box::new(ssrf::SsrfPipeline::new()?),
    ])
}
```
Update scalability_pipelines() to return empty vec.

**Step 3: Add integration tests (14 tests: 7 positive + 7 negative).**
Use Language::Php and ".php" files.

Key fixtures:
- command_injection positive: `<?php\nfunction f($cmd) { system($cmd); }`
- unsafe_include positive: `<?php\nfunction f($path) { include($path); }`
- type_juggling positive: `<?php\nfunction f($a, $b) { if ($a == $b) { return true; } }`
- memory_leak positive: `<?php\nfunction f() { $fh = fopen('file.txt', 'r'); }`

Run `cargo test`.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 Rust files deleted from php/
    - sql_injection.rs and ssrf.rs STILL EXIST
    - php/mod.rs security_pipelines() returns vec with only sql_injection and ssrf
    - cargo test passes with zero failures
  </acceptance_criteria>
  <done>7 PHP Rust files deleted, mod.rs updated preserving 2 exceptions, integration tests added, cargo test passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files -> executor | Compile-time embedded JSON; no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | src/audit/builtin/*_php.json | accept | Same as T-04-01 |
</threat_model>

<verification>
1. `cargo test` passes
2. sql_injection.rs and ssrf.rs preserved
</verification>

<success_criteria>
- 7 JSON created, 7 Rust deleted, 2 exceptions preserved, tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-per-language-scalability-migration/04-09-SUMMARY.md`
</output>
