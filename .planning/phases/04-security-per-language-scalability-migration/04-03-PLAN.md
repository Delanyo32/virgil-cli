---
phase: 04-security-per-language-scalability-migration
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/audit/builtin/command_injection_go.json
  - src/audit/builtin/go_path_traversal_go.json
  - src/audit/builtin/race_conditions_go.json
  - src/audit/builtin/resource_exhaustion_go.json
  - src/audit/builtin/go_integer_overflow_go.json
  - src/audit/builtin/go_type_confusion_go.json
  - src/audit/builtin/memory_leak_indicators_go.json
  - src/audit/pipelines/go/mod.rs
  - tests/audit_json_integration.rs
autonomous: true
requirements: [SEC-01, SEC-02, SCAL-02, SCAL-03, TEST-01, TEST-02]

must_haves:
  truths:
    - "Go security pipelines produce findings via JSON for command_injection, go_path_traversal, race_conditions, resource_exhaustion, go_integer_overflow, go_type_confusion"
    - "Go memory_leak_indicators produces findings via JSON"
    - "sql_injection and ssrf_open_redirect remain as Rust pipelines (permanent exceptions)"
    - "cargo test passes with zero failures"
  artifacts:
    - path: "src/audit/builtin/command_injection_go.json"
      provides: "JSON security pipeline for Go command injection"
      contains: "\"pipeline\": \"command_injection\""
    - path: "src/audit/builtin/race_conditions_go.json"
      provides: "JSON security pipeline for Go race conditions"
      contains: "\"pipeline\": \"race_conditions\""
    - path: "src/audit/builtin/memory_leak_indicators_go.json"
      provides: "JSON scalability pipeline for Go memory leak indicators"
      contains: "\"pipeline\": \"memory_leak_indicators\""
  key_links:
    - from: "src/audit/builtin/*_go.json"
      to: "src/audit/engine.rs"
      via: "include_dir auto-discovery + name-match suppression (ENG-01)"
      pattern: "json_pipeline_names.contains"
---

<objective>
Migrate 6 Go security pipelines and the memory_leak_indicators scalability pipeline from Rust to JSON. Leave sql_injection and ssrf_open_redirect as permanent Rust exceptions.

Purpose: Go has straightforward tree-sitter patterns with well-prefixed pipeline names (go_path_traversal, go_integer_overflow, go_type_confusion). CRITICAL: Go's race_conditions and resource_exhaustion pipelines return fn name() = "race_conditions" / "resource_exhaustion" (same as Rust) -- JSON files MUST include "languages": ["go"] to avoid conflict.
Output: 7 JSON files, 7 Rust .rs files deleted, integration tests.

**PERMANENT RUST EXCEPTIONS:**
- `sql_injection` -- requires FlowsTo/SanitizedBy graph predicates
- `ssrf_open_redirect` -- requires FlowsTo/SanitizedBy graph predicates
</objective>

<execution_context>
@.planning/phases/04-security-per-language-scalability-migration/04-PATTERNS.md
@.planning/phases/04-security-per-language-scalability-migration/04-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/audit/builtin/sync_blocking_in_async_go.json

<interfaces>
From src/audit/pipelines/go/mod.rs security_pipelines():
- command_injection::CommandInjectionPipeline -> name() = "command_injection"
- sql_injection::SqlInjectionPipeline -> KEEP (permanent exception)
- go_path_traversal::GoPathTraversalPipeline -> name() = "go_path_traversal"
- go_race_conditions::GoRaceConditionsPipeline -> name() = "race_conditions" (SAME as Rust!)
- go_resource_exhaustion::GoResourceExhaustionPipeline -> name() = "resource_exhaustion" (SAME as Rust!)
- go_integer_overflow::GoIntegerOverflowPipeline -> name() = "go_integer_overflow"
- go_type_confusion::GoTypeConfusionPipeline -> name() = "go_type_confusion"
- ssrf_open_redirect::SsrfOpenRedirectPipeline -> KEEP (permanent exception)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 JSON pipeline files for Go security + scalability</name>
  <files>
    src/audit/builtin/command_injection_go.json,
    src/audit/builtin/go_path_traversal_go.json,
    src/audit/builtin/race_conditions_go.json,
    src/audit/builtin/resource_exhaustion_go.json,
    src/audit/builtin/go_integer_overflow_go.json,
    src/audit/builtin/go_type_confusion_go.json,
    src/audit/builtin/memory_leak_indicators_go.json
  </files>
  <read_first>
    src/audit/builtin/sync_blocking_in_async_go.json,
    src/audit/pipelines/go/command_injection.rs,
    src/audit/pipelines/go/go_path_traversal.rs,
    src/audit/pipelines/go/go_race_conditions.rs,
    src/audit/pipelines/go/go_resource_exhaustion.rs,
    src/audit/pipelines/go/go_integer_overflow.rs,
    src/audit/pipelines/go/go_type_confusion.rs,
    src/audit/pipelines/go/memory_leak_indicators.rs
  </read_first>
  <action>
Create 7 JSON files. Read each Rust source file, extract tree-sitter patterns, translate to match_pattern. All files use "languages": ["go"].

CRITICAL pipeline name mapping:
- command_injection_go.json: "pipeline": "command_injection", "languages": ["go"]
- go_path_traversal_go.json: "pipeline": "go_path_traversal", "languages": ["go"]
- race_conditions_go.json: "pipeline": "race_conditions", "languages": ["go"] (SAME name as Rust -- languages field disambiguates)
- resource_exhaustion_go.json: "pipeline": "resource_exhaustion", "languages": ["go"] (SAME name as Rust)
- go_integer_overflow_go.json: "pipeline": "go_integer_overflow", "languages": ["go"]
- go_type_confusion_go.json: "pipeline": "go_type_confusion", "languages": ["go"]
- memory_leak_indicators_go.json: "pipeline": "memory_leak_indicators", "languages": ["go"]

For each pipeline, read the Rust source and translate the detection logic:

1. **command_injection** -- Rust detects exec.Command calls. Match `(call_expression function: (selector_expression) @fn)` for Go. Pattern: "exec_command_injection", severity: "error".

2. **go_path_traversal** -- Rust detects filepath.Join. Match selector call to Join on filepath package. Pattern: "unvalidated_path_join", severity: "warning".

3. **race_conditions** (Go) -- Rust detects go_statement inside for_statement. Match `(for_statement body: (block (go_statement) @go))`. Pattern: "loop_var_capture", severity: "warning".

4. **resource_exhaustion** (Go) -- Rust detects goroutine in loop + ioutil.ReadAll/io.ReadAll. Simplified: match go_statement inside for_statement + selector calls broadly. Pattern: "unbounded_goroutine_spawn" / "unbounded_body_read", severity: "warning".

5. **go_integer_overflow** -- Read Rust source for exact patterns. Match type conversion patterns. Pattern from Rust fn name(). Severity: "warning".

6. **go_type_confusion** -- Read Rust source for exact patterns. Match unsafe.Pointer casts. Pattern from Rust fn name(). Severity: "warning".

7. **memory_leak_indicators** (Go) -- Rust detects goroutine in loop, append() in loop, defer in loop. HIGH feasibility. Match for_statement bodies with go_statement, call to append, defer_statement. Pattern: "potential_memory_leak", severity: "warning".

Run `cargo test` after creating all files.
  </action>
  <verify>
    <automated>cargo test --test audit_json_integration 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 JSON files exist in src/audit/builtin/
    - grep '"pipeline": "race_conditions"' src/audit/builtin/race_conditions_go.json returns match
    - grep '"languages"' src/audit/builtin/race_conditions_go.json shows ["go"]
    - grep '"pipeline": "resource_exhaustion"' src/audit/builtin/resource_exhaustion_go.json returns match
    - cargo test compiles without errors
  </acceptance_criteria>
  <done>7 Go JSON pipeline files created, cargo test passes</done>
</task>

<task type="auto">
  <name>Task 2: Delete Go Rust pipeline files + add integration tests</name>
  <files>
    src/audit/pipelines/go/mod.rs,
    tests/audit_json_integration.rs
  </files>
  <read_first>
    src/audit/pipelines/go/mod.rs,
    tests/audit_json_integration.rs
  </read_first>
  <action>
**Step 1: Delete 7 Rust pipeline files from go/:**
Delete: command_injection.rs, go_path_traversal.rs, go_race_conditions.rs, go_resource_exhaustion.rs, go_integer_overflow.rs, go_type_confusion.rs, memory_leak_indicators.rs

**DO NOT delete:** sql_injection.rs, ssrf_open_redirect.rs (permanent exceptions)

**Step 2: Update go/mod.rs:**
Remove `pub mod` lines for all 7 deleted modules. Keep sql_injection and ssrf_open_redirect.
Update security_pipelines() to return only permanent exceptions:
```rust
pub fn security_pipelines() -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![
        Box::new(sql_injection::SqlInjectionPipeline::new()?),
        Box::new(ssrf_open_redirect::SsrfOpenRedirectPipeline::new()?),
    ])
}
```
Update scalability_pipelines() to return empty vec.

**Step 3: Add integration tests (14 tests: 7 positive + 7 negative):**
Add test pairs for each migrated pipeline using Language::Go and ".go" files.

Key fixtures:
- command_injection positive: `package main\nimport "os/exec"\nfunc f(cmd string) { exec.Command(cmd) }`
- race_conditions positive: `package main\nfunc f() { for i := 0; i < 10; i++ { go func(){} () } }`
- memory_leak_indicators positive: `package main\nfunc f() { for i := 0; i < 10; i++ { go func(){} () } }`

Run `cargo test`.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 7 Rust files deleted from src/audit/pipelines/go/
    - sql_injection.rs and ssrf_open_redirect.rs STILL EXIST
    - go/mod.rs security_pipelines() returns vec with only sql_injection and ssrf_open_redirect
    - tests contain "command_injection" and "race_conditions" Go test functions
    - cargo test passes with zero failures
  </acceptance_criteria>
  <done>7 Go Rust files deleted, mod.rs updated, 14 integration tests added, cargo test passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files -> executor | Compile-time embedded JSON; no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | src/audit/builtin/*_go.json | accept | Same as T-04-01 |
</threat_model>

<verification>
1. `cargo test` passes
2. sql_injection.rs and ssrf_open_redirect.rs preserved
3. Go security + scalability JSON tests pass
</verification>

<success_criteria>
- 7 JSON files created, 7 Rust files deleted
- Permanent exceptions preserved
- Integration tests passing
- cargo test zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-per-language-scalability-migration/04-03-SUMMARY.md`
</output>
