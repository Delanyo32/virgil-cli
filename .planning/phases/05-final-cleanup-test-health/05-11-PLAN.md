---
phase: 05-final-cleanup-test-health
plan: 11
type: execute
wave: 2
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - src/audit/pipelines/helpers.rs
  - src/audit/pipelines/mod.rs
  - src/audit/pipelines/csharp/csharp_ssrf.rs
  - src/audit/pipelines/csharp/sql_injection.rs
  - src/audit/pipelines/csharp/xxe.rs
  - src/audit/pipelines/go/sql_injection.rs
  - src/audit/pipelines/go/ssrf_open_redirect.rs
  - src/audit/pipelines/java/java_ssrf.rs
  - src/audit/pipelines/java/sql_injection.rs
  - src/audit/pipelines/java/xxe.rs
  - src/audit/pipelines/javascript/xss_dom_injection.rs
  - src/audit/pipelines/javascript/ssrf.rs
  - src/audit/pipelines/php/sql_injection.rs
  - src/audit/pipelines/php/ssrf.rs
  - src/audit/pipelines/python/sql_injection.rs
  - src/audit/pipelines/python/ssrf.rs
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, TEST-02]

must_haves:
  truths:
    - "helpers.rs contains only functions with verified non-pipeline callers"
    - "src/audit/pipelines/ contains no dead language subdirectories (rust/, c/, cpp/ deleted)"
    - "All 14 taint exception files have PERMANENT RUST EXCEPTION comment at top"
    - "src/audit/pipelines/mod.rs no longer declares pub mod for deleted language dirs"
    - "cargo test passes with zero failures and cargo build has no unused warnings in audit/"
    - "virgil audit produces non-empty output for all categories"
  artifacts:
    - path: "src/audit/pipelines/helpers.rs"
      provides: "Pruned helpers with only live functions"
      contains: "count_all_identifier_occurrences"
    - path: "src/audit/pipelines/mod.rs"
      provides: "Shrunk module declarations"
  key_links:
    - from: "src/audit/pipelines/helpers.rs"
      to: "src/audit/engine.rs"
      via: "count_all_identifier_occurrences import"
      pattern: "helpers::count_all_identifier"
    - from: "src/audit/pipelines/helpers.rs"
      to: "src/graph/executor.rs"
      via: "is_barrel_file, is_excluded_for_arch_analysis, is_test_file imports"
      pattern: "helpers::is_barrel_file"
---

<objective>
Final cleanup after all 10 language migration plans complete: prune dead functions from helpers.rs, delete dead language subdirectories (rust/, c/, cpp/), add PERMANENT RUST EXCEPTION comments to 14 taint files, update top-level mod.rs, and verify the final codebase state.

Purpose: Satisfy CLEAN-01, CLEAN-02, CLEAN-03. Leave the codebase in its final milestone state with zero dead code in the audit pipeline layer.
Output: Pruned helpers.rs, cleaned mod.rs, annotated taint files, passing test suite.
</objective>

<execution_context>
@.planning/phases/05-final-cleanup-test-health/05-PATTERNS.md
@.planning/phases/05-final-cleanup-test-health/05-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From RESEARCH.md -- helpers.rs functions that MUST survive (confirmed non-pipeline callers):

| Function | Non-Pipeline Caller | Location |
|----------|--------------------|----|
| count_all_identifier_occurrences | AuditEngine::run() | src/audit/engine.rs |
| is_barrel_file | execute_match_pattern(), CouplingAnalyzer | src/graph/executor.rs, src/audit/analyzers/coupling.rs |
| is_excluded_for_arch_analysis | execute_match_pattern(), CouplingAnalyzer | src/graph/executor.rs, src/audit/analyzers/coupling.rs |
| is_test_file | execute_match_pattern() | src/graph/executor.rs |
| pub use crate::graph::metrics::* | Re-exports (verify callers before deleting) | helpers.rs lines 7-14 |

All ~49 other pub fn declarations become dead after pipeline files are deleted.

Dead language directories (no taint exceptions, all pipeline files deleted):
- src/audit/pipelines/rust/ (entire directory)
- src/audit/pipelines/c/ (entire directory)
- src/audit/pipelines/cpp/ (entire directory)

TypeScript decision: typescript/mod.rs must stay because it provides security_pipelines delegation to javascript module. The directory stays with only mod.rs.

14 taint exception files to annotate:
- csharp/csharp_ssrf.rs, csharp/sql_injection.rs, csharp/xxe.rs
- go/sql_injection.rs, go/ssrf_open_redirect.rs
- java/java_ssrf.rs, java/sql_injection.rs, java/xxe.rs
- javascript/xss_dom_injection.rs, javascript/ssrf.rs
- php/sql_injection.rs, php/ssrf.rs
- python/sql_injection.rs, python/ssrf.rs

Expected final src/audit/pipelines/mod.rs:
```rust
pub mod csharp;
pub mod go;
pub mod helpers;
pub mod java;
pub mod javascript;
pub mod php;
pub mod python;
pub mod typescript;
// rust, c, cpp DELETED
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead language dirs, prune helpers.rs, update mod.rs</name>
  <files>
    src/audit/pipelines/helpers.rs,
    src/audit/pipelines/mod.rs
  </files>
  <read_first>
    src/audit/pipelines/helpers.rs,
    src/audit/pipelines/mod.rs,
    src/audit/engine.rs (lines containing "helpers::"),
    src/graph/executor.rs (lines containing "helpers::"),
    src/audit/analyzers/coupling.rs (lines containing "helpers::")
  </read_first>
  <action>
**Step 1: Delete dead language subdirectories**

Delete the entire src/audit/pipelines/rust/ directory (all files: mod.rs, primitives.rs -- all pipeline .rs files were deleted in Plan 01).
Delete the entire src/audit/pipelines/c/ directory (all files deleted in Plan 06).
Delete the entire src/audit/pipelines/cpp/ directory (all files deleted in Plan 07).

**Step 2: Update src/audit/pipelines/mod.rs**

Remove these lines:
```rust
pub mod c;
pub mod cpp;
pub mod rust;
```

The final mod.rs should contain only:
```rust
pub mod csharp;
pub mod go;
pub mod helpers;
pub mod java;
pub mod javascript;
pub mod php;
pub mod python;
pub mod typescript;
```

Run `cargo build` to verify compilation after directory deletions.

**Step 3: Prune dead functions from helpers.rs**

Read helpers.rs completely. For EVERY `pub fn` declaration, run:
```bash
grep -r "helpers::{fn_name}" src/ --include="*.rs" | grep -v "src/audit/pipelines/"
```

If the grep returns zero non-pipeline results, the function is dead -- delete it.

Functions confirmed to SURVIVE (from RESEARCH.md verified callers):
- `count_all_identifier_occurrences` -- called by engine.rs
- `is_barrel_file` -- called by executor.rs and analyzers/coupling.rs
- `is_excluded_for_arch_analysis` -- called by executor.rs and analyzers/coupling.rs
- `is_test_file` -- called by executor.rs

For the `pub use crate::graph::metrics::*` re-exports (lines 7-14):
Run `grep -r "helpers::compute_\|helpers::count_function\|helpers::compute_comment\|helpers::ControlFlow" src/ --include="*.rs" | grep -v "src/audit/pipelines/"`.
If zero results, these re-exports are also dead and should be removed.
If any non-pipeline file imports via helpers::, keep the re-exports.

Delete ALL other pub fn declarations (~49 functions including):
- count_nodes_of_kind
- count_parameters
- find_unreachable_after
- hash_block_normalized
- body_references_identifier
- All language-specific helper functions (is_test_context_rust, is_test_context_python, etc.)
- All detection helpers (find_duplicate_bodies, find_duplicate_arms, etc.)
- Any other function with zero non-pipeline callers

Also delete any `use` imports at the top of helpers.rs that are only needed by the deleted functions. After pruning, only the necessary imports remain.

Run `cargo build` after pruning to catch any missed callers. Fix if needed.
Run `cargo test` to verify full green.

**Step 4: Fix any remaining warnings**

Run `cargo build 2>&1 | grep "warning:"`. Fix any unused import or dead code warnings in src/audit/. The existing warning about unused variable in any_escape_hatch.rs should be gone (file was deleted in Plan 09).
  </action>
  <verify>
    <automated>cargo test 2>&1 | grep "test result:" && cargo build 2>&1 | grep -c "warning:" || echo "0 warnings"</automated>
  </verify>
  <acceptance_criteria>
    - src/audit/pipelines/rust/ directory does NOT exist
    - src/audit/pipelines/c/ directory does NOT exist
    - src/audit/pipelines/cpp/ directory does NOT exist
    - src/audit/pipelines/mod.rs does NOT contain "pub mod rust" or "pub mod c;" or "pub mod cpp"
    - src/audit/pipelines/mod.rs contains "pub mod helpers"
    - src/audit/pipelines/helpers.rs contains "count_all_identifier_occurrences"
    - src/audit/pipelines/helpers.rs contains "is_barrel_file"
    - src/audit/pipelines/helpers.rs contains "is_test_file"
    - src/audit/pipelines/helpers.rs does NOT contain "count_nodes_of_kind" (dead function example)
    - src/audit/pipelines/helpers.rs does NOT contain "hash_block_normalized" (dead function example)
    - cargo build produces zero warnings about unused code in src/audit/
    - cargo test passes with zero failures
  </acceptance_criteria>
  <done>Dead directories deleted, helpers.rs pruned to ~4 live functions, mod.rs cleaned, zero warnings, cargo test green</done>
</task>

<task type="auto">
  <name>Task 2: Add PERMANENT RUST EXCEPTION comments to 14 taint files</name>
  <files>
    src/audit/pipelines/csharp/csharp_ssrf.rs,
    src/audit/pipelines/csharp/sql_injection.rs,
    src/audit/pipelines/csharp/xxe.rs,
    src/audit/pipelines/go/sql_injection.rs,
    src/audit/pipelines/go/ssrf_open_redirect.rs,
    src/audit/pipelines/java/java_ssrf.rs,
    src/audit/pipelines/java/sql_injection.rs,
    src/audit/pipelines/java/xxe.rs,
    src/audit/pipelines/javascript/xss_dom_injection.rs,
    src/audit/pipelines/javascript/ssrf.rs,
    src/audit/pipelines/php/sql_injection.rs,
    src/audit/pipelines/php/ssrf.rs,
    src/audit/pipelines/python/sql_injection.rs,
    src/audit/pipelines/python/ssrf.rs
  </files>
  <read_first>
    src/audit/pipelines/csharp/csharp_ssrf.rs (first 5 lines),
    src/audit/pipelines/go/sql_injection.rs (first 5 lines),
    src/audit/pipelines/javascript/xss_dom_injection.rs (first 5 lines),
    src/audit/pipelines/python/sql_injection.rs (first 5 lines)
  </read_first>
  <action>
Add the following exact comment block at line 1 of each of the 14 taint-based security files (before any existing `use` statements or code):

```rust
// PERMANENT RUST EXCEPTION: This pipeline requires FlowsTo/SanitizedBy graph
// predicates for taint propagation analysis. These are not expressible in the
// match_pattern JSON DSL. Do not migrate -- this file stays as Rust intentionally.
```

The 14 files to annotate (D-12):
1. src/audit/pipelines/csharp/csharp_ssrf.rs
2. src/audit/pipelines/csharp/sql_injection.rs
3. src/audit/pipelines/csharp/xxe.rs
4. src/audit/pipelines/go/sql_injection.rs
5. src/audit/pipelines/go/ssrf_open_redirect.rs
6. src/audit/pipelines/java/java_ssrf.rs
7. src/audit/pipelines/java/sql_injection.rs
8. src/audit/pipelines/java/xxe.rs
9. src/audit/pipelines/javascript/xss_dom_injection.rs
10. src/audit/pipelines/javascript/ssrf.rs
11. src/audit/pipelines/php/sql_injection.rs
12. src/audit/pipelines/php/ssrf.rs
13. src/audit/pipelines/python/sql_injection.rs
14. src/audit/pipelines/python/ssrf.rs

After adding comments to all 14 files, run `cargo test` to confirm no breakage (comments don't affect compilation, but verify).

Then run a final comprehensive check:
```bash
cargo test 2>&1 | grep "test result:"
cargo build 2>&1 | grep "warning:"
```

Both must show zero failures and zero warnings in src/audit/.
  </action>
  <verify>
    <automated>cargo test 2>&1 | grep "test result:"</automated>
  </verify>
  <acceptance_criteria>
    - grep "PERMANENT RUST EXCEPTION" src/audit/pipelines/csharp/csharp_ssrf.rs returns a match
    - grep "PERMANENT RUST EXCEPTION" src/audit/pipelines/go/sql_injection.rs returns a match
    - grep "PERMANENT RUST EXCEPTION" src/audit/pipelines/java/xxe.rs returns a match
    - grep "PERMANENT RUST EXCEPTION" src/audit/pipelines/javascript/xss_dom_injection.rs returns a match
    - grep "PERMANENT RUST EXCEPTION" src/audit/pipelines/php/ssrf.rs returns a match
    - grep "PERMANENT RUST EXCEPTION" src/audit/pipelines/python/sql_injection.rs returns a match
    - All 14 files have the comment as the first 3 lines
    - cargo test passes with zero failures
    - cargo build produces zero warnings
  </acceptance_criteria>
  <done>14 taint files annotated with PERMANENT RUST EXCEPTION, final cargo test + cargo build clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only deletes dead code and adds comments; no new functionality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Information Disclosure | helpers.rs pruning | accept | Only removing dead functions; live functions verified by grep before deletion. cargo test catches any missed callers. |
</threat_model>

<verification>
1. `cargo test` passes with zero failures
2. `cargo build` produces zero unused-code warnings in src/audit/
3. src/audit/pipelines/ contains only: helpers.rs, mod.rs, and language subdirs with taint exceptions (csharp/, go/, java/, javascript/, php/, python/, typescript/)
4. No Rust pipeline files remain for any category that has been fully migrated to JSON (CLEAN-02)
5. All 14 taint files have PERMANENT RUST EXCEPTION annotations (D-12)
</verification>

<success_criteria>
- Dead language directories (rust/, c/, cpp/) deleted
- helpers.rs pruned to ~4-5 live functions
- mod.rs cleaned to 8 module declarations
- 14 taint files annotated
- Zero compiler warnings in src/audit/
- cargo test passes with zero failures
- CLEAN-01, CLEAN-02, CLEAN-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-final-cleanup-test-health/05-11-SUMMARY.md`
</output>
