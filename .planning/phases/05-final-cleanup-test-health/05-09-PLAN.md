---
phase: 05-final-cleanup-test-health
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audit/builtin/any_escape_hatch_typescript.json
  - src/audit/builtin/enum_usage_typescript.json
  - src/audit/builtin/implicit_any_typescript.json
  - src/audit/builtin/leaking_impl_types_typescript.json
  - src/audit/builtin/mutable_types_typescript.json
  - src/audit/builtin/optional_everything_typescript.json
  - src/audit/builtin/record_string_any_typescript.json
  - src/audit/builtin/type_assertions_typescript.json
  - src/audit/builtin/type_duplication_typescript.json
  - src/audit/builtin/unchecked_index_access_typescript.json
  - src/audit/builtin/unconstrained_generics_typescript.json
  - src/audit/builtin/dead_code_typescript.json
  - src/audit/builtin/duplicate_code_typescript.json
  - src/audit/builtin/coupling_typescript.json
  - src/audit/pipelines/typescript/mod.rs
  - tests/audit_json_integration.rs
autonomous: true
requirements: [CLEAN-02, TEST-02]

must_haves:
  truths:
    - "All 14 TypeScript tech-debt and code-style pipelines produce findings via JSON"
    - "No TypeScript pipeline .rs files remain (TypeScript has zero own taint files -- delegates to JavaScript)"
    - "The entire src/audit/pipelines/typescript/ directory can be deleted in cleanup plan"
    - "cargo test passes with zero failures"
  artifacts:
    - path: "src/audit/builtin/any_escape_hatch_typescript.json"
      provides: "JSON pipeline for TypeScript any escape hatch"
      contains: "\"pipeline\": \"any_escape_hatch\""
    - path: "src/audit/builtin/implicit_any_typescript.json"
      provides: "JSON pipeline for TypeScript implicit any"
      contains: "\"pipeline\": \"implicit_any\""
  key_links:
    - from: "src/audit/builtin/*_typescript.json"
      to: "src/audit/engine.rs"
      via: "include_dir auto-discovery + name-match suppression"
      pattern: "json_pipeline_names.contains"
---

<objective>
Migrate all 14 TypeScript tech-debt and code-style pipelines from Rust to JSON and add 141 integration tests. TypeScript has zero own taint files (security delegates to javascript::security_pipelines). After migration the entire typescript/ dir is dead.

IMPORTANT: TypeScript mod.rs delegates security_pipelines to javascript module. After migration, the typescript/mod.rs still needs security_pipelines delegation. The cleanup plan (05-11) decides whether to keep a minimal typescript/mod.rs or inline the delegation.

Output: 14 JSON files, 14 Rust .rs files deleted, 141 integration tests.
</objective>

<execution_context>
@.planning/phases/05-final-cleanup-test-health/05-PATTERNS.md
@.planning/phases/05-final-cleanup-test-health/05-RESEARCH.md
</execution_context>

<context>
@audit_plans/typescript_tech_debt.md
@src/audit/builtin/panic_dos_rust.json
@tests/audit_json_integration.rs

<interfaces>
TypeScript pipeline files to migrate (14 total):
- Tech-debt (11): any_escape_hatch, enum_usage, implicit_any, leaking_impl_types, mutable_types, optional_everything, record_string_any, type_assertions, type_duplication, unchecked_index_access, unconstrained_generics
- Code-style (3): dead_code, duplicate_code, coupling

TypeScript test counts (D-06):
- any_escape_hatch: 12, enum_usage: 10, implicit_any: 12, leaking_impl_types: 11
- mutable_types: 11, optional_everything: 9, record_string_any: 9
- type_assertions: 10, type_duplication: 9, unchecked_index_access: 11
- unconstrained_generics: 11
- dead_code: 11, duplicate_code: 6, coupling: 9
Total: 141 tests

Language: Language::TypeScript, extension: .ts
No own taint files -- security delegates to javascript module.

TypeScript mod.rs pattern (see PATTERNS.md Pattern 7):
```rust
pub fn security_pipelines(language: Language) -> Result<Vec<Box<dyn Pipeline>>> {
    pipelines::javascript::security_pipelines(language)
}
```
This delegation MUST be preserved.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 14 TypeScript JSON pipelines, delete Rust files, update mod.rs</name>
  <files>
    src/audit/builtin/any_escape_hatch_typescript.json,
    src/audit/builtin/enum_usage_typescript.json,
    src/audit/builtin/implicit_any_typescript.json,
    src/audit/builtin/leaking_impl_types_typescript.json,
    src/audit/builtin/mutable_types_typescript.json,
    src/audit/builtin/optional_everything_typescript.json,
    src/audit/builtin/record_string_any_typescript.json,
    src/audit/builtin/type_assertions_typescript.json,
    src/audit/builtin/type_duplication_typescript.json,
    src/audit/builtin/unchecked_index_access_typescript.json,
    src/audit/builtin/unconstrained_generics_typescript.json,
    src/audit/builtin/dead_code_typescript.json,
    src/audit/builtin/duplicate_code_typescript.json,
    src/audit/builtin/coupling_typescript.json,
    src/audit/pipelines/typescript/mod.rs
  </files>
  <read_first>
    audit_plans/typescript_tech_debt.md,
    src/audit/pipelines/typescript/any_escape_hatch.rs,
    src/audit/pipelines/typescript/enum_usage.rs,
    src/audit/pipelines/typescript/implicit_any.rs,
    src/audit/pipelines/typescript/leaking_impl_types.rs,
    src/audit/pipelines/typescript/mutable_types.rs,
    src/audit/pipelines/typescript/optional_everything.rs,
    src/audit/pipelines/typescript/record_string_any.rs,
    src/audit/pipelines/typescript/type_assertions.rs,
    src/audit/pipelines/typescript/type_duplication.rs,
    src/audit/pipelines/typescript/unchecked_index_access.rs,
    src/audit/pipelines/typescript/unconstrained_generics.rs,
    src/audit/pipelines/typescript/dead_code.rs,
    src/audit/pipelines/typescript/duplicate_code.rs,
    src/audit/pipelines/typescript/coupling.rs,
    src/audit/pipelines/typescript/mod.rs
  </read_first>
  <action>
Read audit_plans/typescript_tech_debt.md and each Rust file. Create 14 JSON pipelines. "category": "code-quality", "languages": ["typescript"].

Key TS-specific patterns:
- **any_escape_hatch**: match `any` type annotations: `(type_identifier) @ty` for "any"
- **implicit_any**: match function parameters without type annotations
- **type_assertions**: match `as` type assertions: `(as_expression) @assertion`
- **enum_usage**: match enum declarations
- **mutable_types**: match mutable array/object patterns
- **unconstrained_generics**: match generic type parameters without constraints

NOTE on language filter: TypeScript pipelines use "languages": ["typescript"]. This is distinct from JavaScript pipelines using "languages": ["javascript"]. Tree-sitter uses the same parser for both but the Language enum routes correctly.

After JSON: cargo test. Delete 14 .rs files + primitives.rs from typescript/.

Update typescript/mod.rs to minimal form:
```rust
// All non-taint tech-debt + code-style pipelines migrated to JSON.
// Security pipelines delegated to javascript module (taint-based exceptions).

use crate::audit::pipeline::{AnyPipeline, Pipeline};
use crate::audit::pipelines;
use crate::language::Language;
use anyhow::Result;

pub fn tech_debt_pipelines(_language: Language) -> Result<Vec<AnyPipeline>> {
    Ok(vec![])
}

pub fn complexity_pipelines(_language: Language) -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}

pub fn code_style_pipelines(_language: Language) -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}

pub fn security_pipelines(language: Language) -> Result<Vec<Box<dyn Pipeline>>> {
    pipelines::javascript::security_pipelines(language)
}

pub fn scalability_pipelines(_language: Language) -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}
```

CRITICAL: Check the current function signatures in typescript/mod.rs. Some take `language: Language` parameter, some don't. Match the existing signatures exactly to avoid compile errors. The security_pipelines delegation to javascript MUST be preserved.

Cargo test after deletions.
  </action>
  <verify>
    <automated>cargo test 2>&1 | grep "test result:"</automated>
  </verify>
  <acceptance_criteria>
    - 14 JSON files exist with "_typescript.json" suffix
    - Each has "languages": ["typescript"]
    - None of the 14 deleted .rs files exist
    - src/audit/pipelines/typescript/primitives.rs does NOT exist
    - src/audit/pipelines/typescript/mod.rs contains "security_pipelines" delegating to "javascript::security_pipelines"
    - src/audit/pipelines/typescript/mod.rs does NOT contain "pub mod any_escape_hatch" or any of the deleted modules
    - cargo test passes
  </acceptance_criteria>
  <done>14 JSON files created, 14 Rust files + primitives.rs deleted, mod.rs minimal with security delegation, cargo test green</done>
</task>

<task type="auto">
  <name>Task 2: Add 141 integration tests matching TypeScript test depth</name>
  <files>tests/audit_json_integration.rs</files>
  <read_first>
    tests/audit_json_integration.rs (last 50 lines),
    src/audit/pipelines/typescript/any_escape_hatch.rs (tests via git show if deleted),
    src/audit/pipelines/typescript/implicit_any.rs,
    src/audit/pipelines/typescript/dead_code.rs,
    src/audit/builtin/any_escape_hatch_typescript.json,
    src/audit/builtin/implicit_any_typescript.json
  </read_first>
  <action>
Add 141 integration tests with header: `// -- Phase 5: TypeScript Tech Debt + Code Style Pipelines --`

Test counts (D-06):
- any_escape_hatch: 12, enum_usage: 10, implicit_any: 12, leaking_impl_types: 11
- mutable_types: 11, optional_everything: 9, record_string_any: 9
- type_assertions: 10, type_duplication: 9, unchecked_index_access: 11
- unconstrained_generics: 11
- dead_code: 11, duplicate_code: 6, coupling: 9

Use Language::TypeScript, extension ".ts".
  </action>
  <verify>
    <automated>cargo test 2>&1 | grep "test result:"</automated>
  </verify>
  <acceptance_criteria>
    - tests/audit_json_integration.rs contains "Phase 5: TypeScript Tech Debt"
    - At least 12 functions with "any_escape_hatch" in name
    - At least 12 functions with "implicit_any" in name
    - cargo test passes
  </acceptance_criteria>
  <done>141 integration tests added, cargo test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files -> executor | Embedded at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | builtin/*.json | accept | Compiled into binary. |
</threat_model>

<verification>
1. `cargo test` passes with zero failures
2. Security delegation from typescript to javascript still works
3. All TS pipeline functions return empty vecs except security
</verification>

<success_criteria>
- 14 JSON files created, 14 Rust files + primitives.rs deleted
- TypeScript security delegation preserved
- 141 integration tests passing
- cargo test green
</success_criteria>

<output>
After completion, create `.planning/phases/05-final-cleanup-test-health/05-09-SUMMARY.md`
</output>
