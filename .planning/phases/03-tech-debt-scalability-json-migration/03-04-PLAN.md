---
phase: 03-tech-debt-scalability-json-migration
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - tests/audit_json_integration.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Each of the 6 migrated pipelines has at least 1 positive-case integration test that produces a finding"
    - "Each of the 6 migrated pipelines has at least 1 negative-case integration test that produces no finding"
    - "All integration tests exercise the full AuditEngine end-to-end path"
    - "cargo test passes with zero failures including all new tests"
  artifacts:
    - path: "tests/audit_json_integration.rs"
      provides: "12 new test functions (6 pipelines x 2 tests) for Phase 3 JSON pipelines"
      contains: "cyclomatic_complexity"
  key_links:
    - from: "tests/audit_json_integration.rs"
      to: "src/audit/engine.rs AuditEngine::run()"
      via: "End-to-end integration test path"
      pattern: "AuditEngine::new"
---

<objective>
Add 12 integration tests (6 pipelines x 1 positive + 1 negative) to tests/audit_json_integration.rs for the Phase 3 JSON pipelines.

Purpose: Per D-10/TEST-01, each pipeline deletion batch requires at least 1 positive-case and 1 negative-case integration test. These tests exercise the full AuditEngine path (workspace loading -> graph building -> JSON pipeline execution -> finding verification), confirming the JSON pipelines produce correct results after the Rust files were deleted.

Output: 12 new test functions appended to tests/audit_json_integration.rs, all passing, full test suite green.
</objective>

<execution_context>
@.planning/phases/03-tech-debt-scalability-json-migration/03-RESEARCH.md
@.planning/phases/03-tech-debt-scalability-json-migration/03-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From tests/audit_json_integration.rs (existing test pattern, lines 24-47):
```rust
#[test]
fn module_size_distribution_rust_finds_oversized() {
    let dir = tempfile::tempdir().unwrap();
    let content: String = (0..31)
        .map(|i| format!("pub fn func_{i}() {{}}\n"))
        .collect();
    std::fs::write(dir.path().join("lib.rs"), content).unwrap();
    let workspace = Workspace::load(dir.path(), &[Language::Rust], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::Rust]).build().unwrap();
    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::Rust])
        .pipeline_selector(PipelineSelector::Architecture)
        .run(&workspace, Some(&graph))
        .unwrap();
    assert!(
        findings.iter().any(|f| f.pattern == "oversized_module"),
        "expected oversized_module finding; got: {:?}",
        findings.iter().map(|f| &f.pattern).collect::<Vec<_>>()
    );
}
```

From src/audit/engine.rs (PipelineSelector enum):
- PipelineSelector::Complexity -- runs complexity_pipelines() + JSON complexity category
- PipelineSelector::Scalability -- runs scalability_pipelines() + JSON scalability category
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 12 integration tests for Phase 3 JSON pipelines</name>
  <files>tests/audit_json_integration.rs</files>
  <read_first>
    - tests/audit_json_integration.rs (read full file -- understand existing imports at lines 15-20, existing test pattern at lines 24-47, total line count ~270)
    - src/audit/builtin/cyclomatic_complexity.json (verify pattern name used in flag: "high_cyclomatic_complexity")
    - src/audit/builtin/function_length.json (verify pattern name: "function_too_long")
    - src/audit/builtin/cognitive_complexity.json (verify pattern name: "high_cognitive_complexity")
    - src/audit/builtin/comment_to_code_ratio.json (verify pattern name: "comment_ratio_violation")
    - src/audit/builtin/n_plus_one_queries.json (verify pattern name: "query_in_loop")
    - src/audit/builtin/sync_blocking_in_async_typescript.json (verify pattern name: "sync_call_in_async")
  </read_first>
  <action>
Append 12 new test functions to `tests/audit_json_integration.rs` after the existing tests. The existing imports (Workspace, GraphBuilder, AuditEngine, PipelineSelector, Language) are already present -- do NOT duplicate them.

Add a section comment before the new tests:
```rust
// ── Phase 3: Complexity Pipelines (compute_metric) ──
```

**Test 1: cyclomatic_complexity positive case (TypeScript)**
Create a TypeScript function with 12 if-statements (CC = 13 > 10):
```rust
#[test]
fn cyclomatic_complexity_ts_finds_complex_function() {
    let dir = tempfile::tempdir().unwrap();
    // 12 if-statements = CC of 13 (1 base + 12 decision points), exceeds threshold of 10
    let mut content = String::from("export function complex(x: number) {\n");
    for i in 0..12 {
        content.push_str(&format!("  if (x > {i}) {{ console.log({i}); }}\n"));
    }
    content.push_str("}\n");
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        findings.iter().any(|f| f.pipeline == "cyclomatic_complexity" && f.pattern == "high_cyclomatic_complexity"),
        "expected high_cyclomatic_complexity finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 2: cyclomatic_complexity negative case (TypeScript)**
Simple function with 1 if (CC = 2, below threshold 10):
```rust
#[test]
fn cyclomatic_complexity_ts_clean_function() {
    let dir = tempfile::tempdir().unwrap();
    let content = "export function simple(x: number) {\n  if (x > 0) { return x; }\n  return 0;\n}\n";
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        !findings.iter().any(|f| f.pipeline == "cyclomatic_complexity" && f.pattern == "high_cyclomatic_complexity"),
        "expected no high_cyclomatic_complexity finding for simple function; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 3: function_length positive case (TypeScript)**
Function with 55 lines of statements (> 50 threshold):
```rust
#[test]
fn function_length_ts_finds_long_function() {
    let dir = tempfile::tempdir().unwrap();
    let mut content = String::from("export function longFunc() {\n");
    for i in 0..55 {
        content.push_str(&format!("  const x{i} = {i};\n"));
    }
    content.push_str("}\n");
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        findings.iter().any(|f| f.pipeline == "function_length" && f.pattern == "function_too_long"),
        "expected function_too_long finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 4: function_length negative case (TypeScript)**
```rust
#[test]
fn function_length_ts_clean_function() {
    let dir = tempfile::tempdir().unwrap();
    let content = "export function short() {\n  return 1;\n}\n";
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        !findings.iter().any(|f| f.pipeline == "function_length" && f.pattern == "function_too_long"),
        "expected no function_too_long finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 5: cognitive_complexity positive case (TypeScript)**
Deeply nested if/for/while structure (cognitive > 15):
```rust
#[test]
fn cognitive_complexity_ts_finds_complex_function() {
    let dir = tempfile::tempdir().unwrap();
    // Deeply nested: if > for > while > if > if > if produces cognitive complexity well above 15
    let content = r#"export function deepNest(x: number) {
  if (x > 0) {
    for (let i = 0; i < x; i++) {
      while (i > 0) {
        if (i % 2 === 0) {
          if (i % 3 === 0) {
            if (i % 5 === 0) {
              console.log(i);
            }
          }
        }
      }
    }
  }
  if (x > 1) {
    if (x > 2) {
      if (x > 3) {
        console.log(x);
      }
    }
  }
}
"#;
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        findings.iter().any(|f| f.pipeline == "cognitive_complexity" && f.pattern == "high_cognitive_complexity"),
        "expected high_cognitive_complexity finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 6: cognitive_complexity negative case (TypeScript)**
```rust
#[test]
fn cognitive_complexity_ts_clean_function() {
    let dir = tempfile::tempdir().unwrap();
    let content = "export function simple(x: number) {\n  if (x > 0) { return x; }\n  return 0;\n}\n";
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        !findings.iter().any(|f| f.pipeline == "cognitive_complexity" && f.pattern == "high_cognitive_complexity"),
        "expected no high_cognitive_complexity finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 7: comment_to_code_ratio positive case (TypeScript)**
File with 30 lines of code and zero comments (ratio = 0% < 5% threshold):
```rust
#[test]
fn comment_to_code_ratio_ts_finds_under_documented() {
    let dir = tempfile::tempdir().unwrap();
    // 30 lines of code, zero comments = 0% ratio, below 5% threshold
    let mut content = String::new();
    for i in 0..30 {
        content.push_str(&format!("const x{i} = {i};\n"));
    }
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        findings.iter().any(|f| f.pipeline == "comment_to_code_ratio" && f.pattern == "comment_ratio_violation"),
        "expected comment_ratio_violation finding for under-documented file; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 8: comment_to_code_ratio negative case (TypeScript)**
File with balanced comments (50% ratio, within 5-60% range):
```rust
#[test]
fn comment_to_code_ratio_ts_clean_file() {
    let dir = tempfile::tempdir().unwrap();
    // 5 comment lines + 5 code lines = 50% ratio, within acceptable range (5%-60%)
    let content = "// comment 1\n// comment 2\n// comment 3\n// comment 4\n// comment 5\nconst a = 1;\nconst b = 2;\nconst c = 3;\nconst d = 4;\nconst e = 5;\n";
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Complexity)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        !findings.iter().any(|f| f.pipeline == "comment_to_code_ratio" && f.pattern == "comment_ratio_violation"),
        "expected no comment_ratio_violation finding for balanced file; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

Add another section comment:
```rust
// ── Phase 3: Scalability Pipelines (match_pattern) ──
```

**Test 9: n_plus_one_queries positive case (TypeScript)**
Call expression inside a for loop:
```rust
#[test]
fn n_plus_one_queries_ts_finds_call_in_loop() {
    let dir = tempfile::tempdir().unwrap();
    let content = r#"
const db = { findOne: (id: number) => ({}) };
const ids = [1, 2, 3];
for (let i = 0; i < ids.length; i++) {
  db.findOne(ids[i]);
}
"#;
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Scalability)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        findings.iter().any(|f| f.pipeline == "n_plus_one_queries" && f.pattern == "query_in_loop"),
        "expected query_in_loop finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 10: n_plus_one_queries negative case (TypeScript)**
Call expression outside any loop:
```rust
#[test]
fn n_plus_one_queries_ts_clean_code() {
    let dir = tempfile::tempdir().unwrap();
    let content = "const db = { findOne: (id: number) => ({}) };\nconst user = db.findOne(1);\n";
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Scalability)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        !findings.iter().any(|f| f.pipeline == "n_plus_one_queries" && f.pattern == "query_in_loop"),
        "expected no query_in_loop finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 11: sync_blocking_in_async positive case (TypeScript)**
Call with Sync-suffix method (readFileSync). Whether this test works depends on whether the `#match?` predicate query compiled successfully in Plan 03. If the JSON pipeline was broadened (no #match?), adjust the fixture to use any member_expression call instead:
```rust
#[test]
fn sync_blocking_in_async_ts_finds_sync_call() {
    let dir = tempfile::tempdir().unwrap();
    let content = "import * as fs from 'fs';\nfs.readFileSync('test.txt');\n";
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Scalability)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        findings.iter().any(|f| f.pipeline == "sync_blocking_in_async" && f.pattern == "sync_call_in_async"),
        "expected sync_call_in_async finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

**Test 12: sync_blocking_in_async negative case (TypeScript)**
Async function using promise-based API (no Sync suffix):
```rust
#[test]
fn sync_blocking_in_async_ts_clean_code() {
    let dir = tempfile::tempdir().unwrap();
    // Only async/promise calls, no Sync suffix methods
    let content = "async function load() {\n  const data = await fetch('https://example.com');\n  return data;\n}\n";
    std::fs::write(dir.path().join("test.ts"), content).unwrap();

    let workspace = Workspace::load(dir.path(), &[Language::TypeScript], Some(10_000_000)).unwrap();
    let graph = GraphBuilder::new(&workspace, &[Language::TypeScript]).build().unwrap();

    let (findings, _) = AuditEngine::new()
        .languages(vec![Language::TypeScript])
        .pipeline_selector(PipelineSelector::Scalability)
        .run(&workspace, Some(&graph))
        .unwrap();

    assert!(
        !findings.iter().any(|f| f.pipeline == "sync_blocking_in_async" && f.pattern == "sync_call_in_async"),
        "expected no sync_call_in_async finding; got: {:?}",
        findings.iter().map(|f| (&f.pipeline, &f.pattern)).collect::<Vec<_>>()
    );
}
```

After adding all 12 tests, run `cargo test --test audit_json_integration` to verify all new tests pass, then `cargo test` for full suite.

**IMPORTANT:** If any test fails because the JSON pipeline pattern name doesn't match (e.g., the executor emits a different pattern name than expected), read the actual JSON file to verify the correct pattern name and adjust the assertion accordingly.
  </action>
  <verify>
    <automated>cd /Users/delanyoaborchie/Documents/github/virgil-cli && cargo test --test audit_json_integration 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/audit_json_integration.rs contains `fn cyclomatic_complexity_ts_finds_complex_function`
    - tests/audit_json_integration.rs contains `fn cyclomatic_complexity_ts_clean_function`
    - tests/audit_json_integration.rs contains `fn function_length_ts_finds_long_function`
    - tests/audit_json_integration.rs contains `fn function_length_ts_clean_function`
    - tests/audit_json_integration.rs contains `fn cognitive_complexity_ts_finds_complex_function`
    - tests/audit_json_integration.rs contains `fn cognitive_complexity_ts_clean_function`
    - tests/audit_json_integration.rs contains `fn comment_to_code_ratio_ts_finds_under_documented`
    - tests/audit_json_integration.rs contains `fn comment_to_code_ratio_ts_clean_file`
    - tests/audit_json_integration.rs contains `fn n_plus_one_queries_ts_finds_call_in_loop`
    - tests/audit_json_integration.rs contains `fn n_plus_one_queries_ts_clean_code`
    - tests/audit_json_integration.rs contains `fn sync_blocking_in_async_ts_finds_sync_call`
    - tests/audit_json_integration.rs contains `fn sync_blocking_in_async_ts_clean_code`
    - cargo test --test audit_json_integration exits 0 (all 20 tests: 8 existing + 12 new)
    - cargo test exits 0 (full suite)
  </acceptance_criteria>
  <done>12 new integration tests added to tests/audit_json_integration.rs (6 positive + 6 negative for all Phase 3 pipelines), all passing, full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test-only file, no production code modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | N/A | tests/audit_json_integration.rs | accept | Test file only -- no security surface. Tempdir fixtures are ephemeral. |
</threat_model>

<verification>
- `cargo test --test audit_json_integration` -- all 20 tests pass (8 existing + 12 new)
- `cargo test` -- zero failures (full suite)
- `grep -c "#\[test\]" tests/audit_json_integration.rs` returns >= 20
</verification>

<success_criteria>
12 integration tests cover all 6 Phase 3 pipelines with positive and negative cases. All tests exercise the full AuditEngine end-to-end path. No test failures in full suite.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tech-debt-scalability-json-migration/03-04-SUMMARY.md`
</output>
