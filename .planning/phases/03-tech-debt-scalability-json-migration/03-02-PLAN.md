---
phase: 03-tech-debt-scalability-json-migration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/audit/builtin/cyclomatic_complexity.json
  - src/audit/builtin/function_length.json
  - src/audit/builtin/cognitive_complexity.json
  - src/audit/builtin/comment_to_code_ratio.json
  - src/audit/pipelines/typescript/mod.rs
  - src/audit/pipelines/javascript/mod.rs
  - src/audit/pipelines/rust/mod.rs
  - src/audit/pipelines/python/mod.rs
  - src/audit/pipelines/go/mod.rs
  - src/audit/pipelines/java/mod.rs
  - src/audit/pipelines/c/mod.rs
  - src/audit/pipelines/cpp/mod.rs
  - src/audit/pipelines/csharp/mod.rs
  - src/audit/pipelines/php/mod.rs
autonomous: true
requirements:
  - TECH-01
  - TECH-03
  - TEST-02
must_haves:
  truths:
    - "Running virgil audit code-quality complexity against any supported language returns cyclomatic_complexity, function_length, cognitive_complexity, and comment_to_code_ratio findings via JSON pipelines"
    - "No Rust pipeline files remain for the 4 complexity pipelines across all 10 language directories"
    - "cargo test passes with zero failures after all complexity Rust files are deleted"
  artifacts:
    - path: "src/audit/builtin/cyclomatic_complexity.json"
      provides: "Cross-language cyclomatic complexity detection via compute_metric stage"
      contains: "compute_metric"
    - path: "src/audit/builtin/function_length.json"
      provides: "Cross-language function length detection via compute_metric stage"
      contains: "compute_metric"
    - path: "src/audit/builtin/cognitive_complexity.json"
      provides: "Cross-language cognitive complexity detection via compute_metric stage"
      contains: "compute_metric"
    - path: "src/audit/builtin/comment_to_code_ratio.json"
      provides: "Cross-language file-level comment ratio detection via compute_metric stage"
      contains: "select.*file"
  key_links:
    - from: "src/audit/builtin/cyclomatic_complexity.json"
      to: "src/graph/executor.rs execute_compute_metric"
      via: "JSON stage dispatch in run_pipeline"
      pattern: "compute_metric.*cyclomatic_complexity"
    - from: "src/audit/engine.rs json_pipeline_names suppression"
      to: "Rust pipeline name match"
      via: "HashSet contains check at line 111"
      pattern: "json_pipeline_names"
---

<objective>
Create 4 cross-language complexity JSON pipelines and delete the 40 replaced Rust pipeline files (4 pipelines x 10 language directories).

Purpose: Per D-02, the 4 complexity pipelines ship as cross-language JSON files (no languages filter). The compute_metric executor stage is language-aware internally, so one JSON file per pipeline replaces 10 language-specific Rust files. Per D-09, Rust files are deleted in the same batch as the JSON replacement.

Output: 4 new JSON files in src/audit/builtin/, 40 Rust .rs files deleted, 10 mod.rs files updated, cargo test passing.
</objective>

<execution_context>
@.planning/phases/03-tech-debt-scalability-json-migration/03-RESEARCH.md
@.planning/phases/03-tech-debt-scalability-json-migration/03-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/graph/executor.rs (execute_compute_metric dispatch, verified in Phase 2):
- Supported metric names: "cyclomatic_complexity", "cognitive_complexity", "function_length", "comment_to_code_ratio"
- For cyclomatic/cognitive/function_length: locates function body at node's start line, computes metric, stores in node.metrics as MetricValue::Int
- For comment_to_code_ratio: special-cases to operate on whole file root (not function body), stores integer percentage (0-100) in node.metrics

From src/graph/pipeline.rs (WhereClause, after Plan 01 extension):
- severity_map when clauses can use: {"cyclomatic_complexity": {"gt": 10}}, {"function_length": {"gt": 50}}, etc.
- select stage where clause can use: {"kind": ["function", "method", "arrow_function"]}

From src/audit/builtin/module_size_distribution_rust.json (reference JSON structure):
```json
{
  "pipeline": "<name>",
  "category": "<category>",
  "description": "<desc>",
  "languages": ["<optional>"],
  "graph": [ ...stages... ]
}
```

From src/audit/engine.rs (suppression mechanism, line 111):
- JSON pipeline names are collected into a HashSet
- Rust pipelines with matching names are suppressed (retain filter)
- Pipeline name in JSON must exactly match Rust Pipeline::name() return value
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 cross-language complexity JSON pipelines</name>
  <files>src/audit/builtin/cyclomatic_complexity.json, src/audit/builtin/function_length.json, src/audit/builtin/cognitive_complexity.json, src/audit/builtin/comment_to_code_ratio.json</files>
  <read_first>
    - src/audit/builtin/module_size_distribution_rust.json (reference JSON structure with severity_map)
    - src/audit/pipelines/typescript/cyclomatic.rs (verify pipeline name() return value -- must match JSON "pipeline" field exactly)
    - src/audit/pipelines/typescript/function_length.rs (verify pipeline name and thresholds)
    - src/audit/pipelines/typescript/cognitive.rs (verify pipeline name and thresholds)
    - src/audit/pipelines/typescript/comment_ratio.rs (verify pipeline name and thresholds)
    - src/graph/executor.rs (lines 789-860 area -- verify execute_compute_metric behavior for each metric)
  </read_first>
  <action>
Create 4 JSON files in `src/audit/builtin/`. Per D-02, these are cross-language (no `"languages"` field). Per D-03, the first 3 use `{"select": "symbol", "where": {"kind": ["function", "method", "arrow_function"]}}` with `{"exclude": {"is_test_file": true}}`. The 4th (comment_to_code_ratio) uses `{"select": "file"}` because it operates at file level.

**CRITICAL:** The `"pipeline"` value in each JSON file MUST exactly match the Rust pipeline's `fn name()` return value. This is the suppression key. Check each Rust file's `fn name()` method to confirm.

**File 1: `src/audit/builtin/cyclomatic_complexity.json`**
Per D-04, CC threshold from audit_plans/ specs: warning > 10, error >= 20 (Rust default CC > 10 is fallback).
```json
{
  "pipeline": "cyclomatic_complexity",
  "category": "code-quality",
  "description": "Detects functions with high cyclomatic complexity. CC = 1 + decision points + logical operators. Threshold: warning > 10, error >= 20.",
  "graph": [
    {
      "select": "symbol",
      "where": {"kind": ["function", "method", "arrow_function"]},
      "exclude": {"is_test_file": true}
    },
    {"compute_metric": "cyclomatic_complexity"},
    {
      "flag": {
        "pattern": "high_cyclomatic_complexity",
        "message": "Function `{{name}}` has cyclomatic complexity of {{cyclomatic_complexity}} (threshold: 10)",
        "severity_map": [
          {"when": {"cyclomatic_complexity": {"gte": 20}}, "severity": "error"},
          {"when": {"cyclomatic_complexity": {"gt": 10}}, "severity": "warning"}
        ]
      }
    }
  ]
}
```

**File 2: `src/audit/builtin/function_length.json`**
Per D-04, threshold from Rust defaults: lines > 50 = warning, lines >= 100 = error. Note: statement count threshold (> 20) is not expressible in JSON DSL -- document in description.
```json
{
  "pipeline": "function_length",
  "category": "code-quality",
  "description": "Detects functions that are too long. Threshold: warning > 50 lines, error >= 100 lines. Note: the Rust implementation also checked statement count (> 20 statements); that check is not expressible in the current JSON DSL and is dropped.",
  "graph": [
    {
      "select": "symbol",
      "where": {"kind": ["function", "method", "arrow_function"]},
      "exclude": {"is_test_file": true}
    },
    {"compute_metric": "function_length"},
    {
      "flag": {
        "pattern": "function_too_long",
        "message": "Function `{{name}}` is {{function_length}} lines long (threshold: 50)",
        "severity_map": [
          {"when": {"function_length": {"gte": 100}}, "severity": "error"},
          {"when": {"function_length": {"gt": 50}}, "severity": "warning"}
        ]
      }
    }
  ]
}
```

**File 3: `src/audit/builtin/cognitive_complexity.json`**
Per D-04, threshold: cognitive > 15 = warning, >= 30 = error.
```json
{
  "pipeline": "cognitive_complexity",
  "category": "code-quality",
  "description": "Detects functions with high cognitive complexity. Cognitive complexity penalizes nesting depth beyond cyclomatic complexity. Threshold: warning > 15, error >= 30.",
  "graph": [
    {
      "select": "symbol",
      "where": {"kind": ["function", "method", "arrow_function"]},
      "exclude": {"is_test_file": true}
    },
    {"compute_metric": "cognitive_complexity"},
    {
      "flag": {
        "pattern": "high_cognitive_complexity",
        "message": "Function `{{name}}` has cognitive complexity of {{cognitive_complexity}} (threshold: 15)",
        "severity_map": [
          {"when": {"cognitive_complexity": {"gte": 30}}, "severity": "error"},
          {"when": {"cognitive_complexity": {"gt": 15}}, "severity": "warning"}
        ]
      }
    }
  ]
}
```

**File 4: `src/audit/builtin/comment_to_code_ratio.json`**
Per RESEARCH.md pattern 2: uses `select: "file"` NOT `select: "symbol"`. Executor stores ratio as integer percentage (0-100). Thresholds: < 5% = under-documented (warning), > 60% = over-documented (warning). Files in 5-60% range produce no finding because no `when` clause matches and no default severity is set.
```json
{
  "pipeline": "comment_to_code_ratio",
  "category": "code-quality",
  "description": "Detects files with too few or too many comments relative to code. Value stored as integer percentage (0-100). Under-documented = ratio < 5%; over-documented = ratio > 60%.",
  "graph": [
    {
      "select": "file",
      "exclude": {"is_test_file": true}
    },
    {"compute_metric": "comment_to_code_ratio"},
    {
      "flag": {
        "pattern": "comment_ratio_violation",
        "message": "File has comment ratio of {{comment_to_code_ratio}}% (expected range: 5%-60%)",
        "severity_map": [
          {"when": {"comment_to_code_ratio": {"lt": 5}}, "severity": "warning"},
          {"when": {"comment_to_code_ratio": {"gt": 60}}, "severity": "warning"}
        ]
      }
    }
  ]
}
```

After creating all 4 files, run `cargo test` to verify the JSON files are discovered by include_dir! and deserialized without error.
  </action>
  <verify>
    <automated>cd /Users/delanyoaborchie/Documents/github/virgil-cli && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/audit/builtin/cyclomatic_complexity.json exists and contains `"pipeline": "cyclomatic_complexity"`
    - src/audit/builtin/cyclomatic_complexity.json contains `"compute_metric": "cyclomatic_complexity"`
    - src/audit/builtin/function_length.json exists and contains `"pipeline": "function_length"`
    - src/audit/builtin/function_length.json contains `"compute_metric": "function_length"`
    - src/audit/builtin/cognitive_complexity.json exists and contains `"pipeline": "cognitive_complexity"`
    - src/audit/builtin/cognitive_complexity.json contains `"compute_metric": "cognitive_complexity"`
    - src/audit/builtin/comment_to_code_ratio.json exists and contains `"pipeline": "comment_to_code_ratio"`
    - src/audit/builtin/comment_to_code_ratio.json contains `"select": "file"` (NOT "symbol")
    - cargo test exits 0
  </acceptance_criteria>
  <done>4 cross-language complexity JSON pipelines created in src/audit/builtin/, all using the correct stage DSL, all auto-discovered by include_dir!, cargo test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete TS/JS/Rust complexity Rust files and update 3 mod.rs files</name>
  <files>src/audit/pipelines/typescript/mod.rs, src/audit/pipelines/javascript/mod.rs, src/audit/pipelines/rust/mod.rs</files>
  <read_first>
    - src/audit/pipelines/typescript/mod.rs (understand complexity_pipelines() factory -- lines 58-65, and pub mod declarations lines 15-18)
    - src/audit/pipelines/javascript/mod.rs (same pattern as TypeScript -- takes `language` arg)
    - src/audit/pipelines/rust/mod.rs (different pattern: complexity_pipelines() returns Vec&lt;Box&lt;dyn Pipeline&gt;&gt; with no language arg, lines 54-61)
  </read_first>
  <action>
**Delete 12 Rust pipeline files** (4 pipelines x 3 language directories: typescript, javascript, rust):

```
rm src/audit/pipelines/typescript/cyclomatic.rs
rm src/audit/pipelines/typescript/function_length.rs
rm src/audit/pipelines/typescript/cognitive.rs
rm src/audit/pipelines/typescript/comment_ratio.rs
rm src/audit/pipelines/javascript/cyclomatic.rs
rm src/audit/pipelines/javascript/function_length.rs
rm src/audit/pipelines/javascript/cognitive.rs
rm src/audit/pipelines/javascript/comment_ratio.rs
rm src/audit/pipelines/rust/cyclomatic.rs
rm src/audit/pipelines/rust/function_length.rs
rm src/audit/pipelines/rust/cognitive.rs
rm src/audit/pipelines/rust/comment_ratio.rs
```

**Update 3 mod.rs files** -- for each, remove these 4 module declarations:
```rust
pub mod cognitive;
pub mod comment_ratio;
pub mod cyclomatic;
pub mod function_length;
```

**Update `complexity_pipelines()` in each mod.rs:**

For TypeScript (`src/audit/pipelines/typescript/mod.rs`):
```rust
pub fn complexity_pipelines(language: Language) -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}
```

For JavaScript (`src/audit/pipelines/javascript/mod.rs`) -- same pattern as TypeScript (takes `language` arg):
```rust
pub fn complexity_pipelines(language: Language) -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}
```

For Rust (`src/audit/pipelines/rust/mod.rs`) -- no language arg:
```rust
pub fn complexity_pipelines() -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}
```

**IMPORTANT:** After removing the 4 entries from `complexity_pipelines()`, check if now-unused imports need to be removed. If `Language` was only used by complexity_pipelines (TypeScript/JavaScript pattern), check if other functions use it too before removing.

After all deletions and mod.rs updates, run `cargo build` first to catch compile errors, then `cargo test`.
  </action>
  <verify>
    <automated>cd /Users/delanyoaborchie/Documents/github/virgil-cli && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/audit/pipelines/typescript/cyclomatic.rs does NOT exist
    - src/audit/pipelines/typescript/cognitive.rs does NOT exist
    - src/audit/pipelines/typescript/function_length.rs does NOT exist
    - src/audit/pipelines/typescript/comment_ratio.rs does NOT exist
    - src/audit/pipelines/javascript/cyclomatic.rs does NOT exist
    - src/audit/pipelines/rust/cyclomatic.rs does NOT exist
    - No mod.rs in {typescript,javascript,rust} contains `pub mod cyclomatic;`
    - Each mod.rs complexity_pipelines() returns Ok(vec![])
    - cargo test exits 0
  </acceptance_criteria>
  <done>12 Rust complexity pipeline files deleted (TS/JS/Rust), 3 mod.rs files updated with empty complexity_pipelines(), cargo test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Delete Python/Go/Java/C complexity Rust files and update 4 mod.rs files</name>
  <files>src/audit/pipelines/python/mod.rs, src/audit/pipelines/go/mod.rs, src/audit/pipelines/java/mod.rs, src/audit/pipelines/c/mod.rs</files>
  <read_first>
    - src/audit/pipelines/python/mod.rs (different pattern: complexity_pipelines() returns Vec&lt;AnyPipeline&gt; with AnyPipeline::Node wrappers, lines 59-65)
    - src/audit/pipelines/go/mod.rs (verify factory signature)
    - src/audit/pipelines/java/mod.rs (verify factory signature)
    - src/audit/pipelines/c/mod.rs (verify factory signature)
  </read_first>
  <action>
**Delete 16 Rust pipeline files** (4 pipelines x 4 language directories: python, go, java, c):

```
rm src/audit/pipelines/python/cyclomatic.rs
rm src/audit/pipelines/python/function_length.rs
rm src/audit/pipelines/python/cognitive.rs
rm src/audit/pipelines/python/comment_ratio.rs
rm src/audit/pipelines/go/cyclomatic.rs
rm src/audit/pipelines/go/function_length.rs
rm src/audit/pipelines/go/cognitive.rs
rm src/audit/pipelines/go/comment_ratio.rs
rm src/audit/pipelines/java/cyclomatic.rs
rm src/audit/pipelines/java/function_length.rs
rm src/audit/pipelines/java/cognitive.rs
rm src/audit/pipelines/java/comment_ratio.rs
rm src/audit/pipelines/c/cyclomatic.rs
rm src/audit/pipelines/c/function_length.rs
rm src/audit/pipelines/c/cognitive.rs
rm src/audit/pipelines/c/comment_ratio.rs
```

**Update 4 mod.rs files** -- for each, remove these 4 module declarations:
```rust
pub mod cognitive;
pub mod comment_ratio;
pub mod cyclomatic;
pub mod function_length;
```

**Update `complexity_pipelines()` in each mod.rs:**

For Python (`src/audit/pipelines/python/mod.rs`) -- no language arg, returns `Vec<AnyPipeline>`:
```rust
pub fn complexity_pipelines() -> Result<Vec<AnyPipeline>> {
    Ok(vec![])
}
```

For Go, Java, C -- no language arg, returns `Vec<Box<dyn Pipeline>>`:
```rust
pub fn complexity_pipelines() -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}
```

After all deletions and mod.rs updates, run `cargo build` then `cargo test`.
  </action>
  <verify>
    <automated>cd /Users/delanyoaborchie/Documents/github/virgil-cli && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/audit/pipelines/python/cyclomatic.rs does NOT exist
    - src/audit/pipelines/go/cyclomatic.rs does NOT exist
    - src/audit/pipelines/java/cyclomatic.rs does NOT exist
    - src/audit/pipelines/c/cyclomatic.rs does NOT exist
    - No mod.rs in {python,go,java,c} contains `pub mod cyclomatic;`
    - Each mod.rs complexity_pipelines() returns Ok(vec![])
    - cargo test exits 0
  </acceptance_criteria>
  <done>16 Rust complexity pipeline files deleted (Python/Go/Java/C), 4 mod.rs files updated with empty complexity_pipelines(), cargo test passes.</done>
</task>

<task type="auto">
  <name>Task 4: Delete C++/C#/PHP complexity Rust files and update 3 mod.rs files</name>
  <files>src/audit/pipelines/cpp/mod.rs, src/audit/pipelines/csharp/mod.rs, src/audit/pipelines/php/mod.rs</files>
  <read_first>
    - src/audit/pipelines/cpp/mod.rs (verify factory signature)
    - src/audit/pipelines/csharp/mod.rs (verify factory signature)
    - src/audit/pipelines/php/mod.rs (verify factory signature)
  </read_first>
  <action>
**Delete 12 Rust pipeline files** (4 pipelines x 3 language directories: cpp, csharp, php):

```
rm src/audit/pipelines/cpp/cyclomatic.rs
rm src/audit/pipelines/cpp/function_length.rs
rm src/audit/pipelines/cpp/cognitive.rs
rm src/audit/pipelines/cpp/comment_ratio.rs
rm src/audit/pipelines/csharp/cyclomatic.rs
rm src/audit/pipelines/csharp/function_length.rs
rm src/audit/pipelines/csharp/cognitive.rs
rm src/audit/pipelines/csharp/comment_ratio.rs
rm src/audit/pipelines/php/cyclomatic.rs
rm src/audit/pipelines/php/function_length.rs
rm src/audit/pipelines/php/cognitive.rs
rm src/audit/pipelines/php/comment_ratio.rs
```

**Update 3 mod.rs files** -- for each, remove these 4 module declarations:
```rust
pub mod cognitive;
pub mod comment_ratio;
pub mod cyclomatic;
pub mod function_length;
```

**Update `complexity_pipelines()` in each mod.rs** -- all three take no language arg, return `Vec<Box<dyn Pipeline>>`:
```rust
pub fn complexity_pipelines() -> Result<Vec<Box<dyn Pipeline>>> {
    Ok(vec![])
}
```

After all deletions and mod.rs updates, run `cargo build` then `cargo test`.
  </action>
  <verify>
    <automated>cd /Users/delanyoaborchie/Documents/github/virgil-cli && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/audit/pipelines/cpp/cyclomatic.rs does NOT exist
    - src/audit/pipelines/csharp/cyclomatic.rs does NOT exist
    - src/audit/pipelines/php/cyclomatic.rs does NOT exist
    - No mod.rs in {cpp,csharp,php} contains `pub mod cyclomatic;`
    - No mod.rs file in any of the 10 language directories contains `pub mod cognitive;`
    - No mod.rs file in any of the 10 language directories contains `pub mod comment_ratio;`
    - No mod.rs file in any of the 10 language directories contains `pub mod function_length;`
    - Each mod.rs complexity_pipelines() returns Ok(vec![])
    - cargo test exits 0
  </acceptance_criteria>
  <done>12 Rust complexity pipeline files deleted (C++/C#/PHP), 3 mod.rs files updated with empty complexity_pipelines(), cargo test passes. All 40 Rust complexity files now deleted across all 10 language directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON pipeline file -> executor stage dispatch | Pipeline DSL stages dispatched to executor functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | JSON pipeline compute_metric stage | accept | Metric names are validated in execute_compute_metric via match arm -- unknown names produce anyhow::bail with descriptive error. Only 4 known metric names are accepted. |
| T-03-04 | Information Disclosure | Metric values in findings | accept | CC/function_length values are code quality metrics, not sensitive data. Findings are returned to the user who invoked the audit. |
</threat_model>

<verification>
- `ls src/audit/builtin/cyclomatic_complexity.json src/audit/builtin/function_length.json src/audit/builtin/cognitive_complexity.json src/audit/builtin/comment_to_code_ratio.json` -- all 4 exist
- `find src/audit/pipelines -name "cyclomatic.rs" -o -name "cognitive.rs" -o -name "function_length.rs" -o -name "comment_ratio.rs"` -- returns empty (all 40 deleted)
- `cargo test` -- zero failures
</verification>

<success_criteria>
4 cross-language complexity JSON pipelines created, 40 Rust pipeline files deleted across 3 batches (TS/JS/Rust, Python/Go/Java/C, C++/C#/PHP), all 10 language mod.rs files updated. The engine's name-match suppression ensures no doubled findings. cargo test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tech-debt-scalability-json-migration/03-02-SUMMARY.md`
</output>
