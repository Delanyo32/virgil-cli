---
phase: 01-engine-fixes-architecture-json-expansion
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/audit/builtin/module_size_distribution_javascript.json
  - src/audit/builtin/module_size_distribution_python.json
  - src/audit/builtin/module_size_distribution_rust.json
  - src/audit/builtin/module_size_distribution_go.json
  - src/audit/builtin/module_size_distribution_java.json
  - src/audit/builtin/circular_dependencies_javascript.json
  - src/audit/builtin/circular_dependencies_python.json
  - src/audit/builtin/circular_dependencies_rust.json
  - src/audit/builtin/circular_dependencies_go.json
  - src/audit/builtin/circular_dependencies_java.json
  - src/audit/builtin/dependency_graph_depth_javascript.json
  - src/audit/builtin/dependency_graph_depth_python.json
  - src/audit/builtin/dependency_graph_depth_rust.json
  - src/audit/builtin/dependency_graph_depth_go.json
  - src/audit/builtin/dependency_graph_depth_java.json
  - src/audit/builtin/api_surface_area_javascript.json
  - src/audit/builtin/api_surface_area_python.json
  - src/audit/builtin/api_surface_area_rust.json
  - src/audit/builtin/api_surface_area_go.json
  - src/audit/builtin/api_surface_area_java.json
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05

must_haves:
  truths:
    - "Running virgil audit architecture against a TypeScript project returns architecture findings"
    - "Running virgil audit architecture against a Python project returns architecture findings"
    - "Running virgil audit architecture against a Rust project returns architecture findings"
    - "Running virgil audit architecture against a Go project returns architecture findings"
    - "Running virgil audit architecture against a Java project returns architecture findings"
  artifacts:
    - path: "src/audit/builtin/module_size_distribution_javascript.json"
      provides: "TS/JS module size distribution pipeline"
      contains: "module_size_distribution"
    - path: "src/audit/builtin/module_size_distribution_python.json"
      provides: "Python module size distribution pipeline"
      contains: "module_size_distribution"
    - path: "src/audit/builtin/module_size_distribution_rust.json"
      provides: "Rust module size distribution pipeline"
      contains: "module_size_distribution"
    - path: "src/audit/builtin/module_size_distribution_go.json"
      provides: "Go module size distribution pipeline"
      contains: "module_size_distribution"
    - path: "src/audit/builtin/module_size_distribution_java.json"
      provides: "Java module size distribution pipeline"
      contains: "module_size_distribution"
  key_links:
    - from: "src/audit/builtin/*.json"
      to: "src/audit/json_audit.rs"
      via: "include_dir! compile-time embedding"
      pattern: "BUILTIN_AUDITS_DIR"
---

<objective>
Create 20 per-language JSON architecture pipeline files for the first 5 language groups (TypeScript/JavaScript, Python, Rust, Go, Java) — 4 pipelines per language group. Each file uses language-calibrated thresholds from the audit_plans/ specs and includes the "languages" filter field.

Purpose: Deliver architecture audit coverage via declarative JSON for 5 of 9 language groups. Per D-01, each file gets a unique pipeline name (`{pipeline}_{lang}`) and per D-04, thresholds are calibrated per language.
Output: 20 new JSON files in `src/audit/builtin/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-fixes-architecture-json-expansion/01-RESEARCH.md
@.planning/phases/01-engine-fixes-architecture-json-expansion/01-PATTERNS.md
@.planning/phases/01-engine-fixes-architecture-json-expansion/01-01-SUMMARY.md

<interfaces>
<!-- Templates to derive from — read these before writing any JSON files -->

From src/audit/builtin/module_size_distribution.json (full template):
```json
{
  "pipeline": "module_size_distribution",
  "category": "architecture",
  "description": "Detect modules that have grown too large (too many symbol definitions)",
  "graph": [
    {"select": "symbol", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}, {"is_barrel_file": true}]}},
    {"group_by": "file"},
    {"count": {"threshold": {"gte": 30}}},
    {"flag": {"pattern": "oversized_module", "message": "Oversized module: {{file}} has {{count}} definitions", "severity_map": [{"when": {"count": {"gte": 100}}, "severity": "error"}, {"when": {"count": {"gte": 50}}, "severity": "warning"}, {"severity": "info"}]}}
  ]
}
```

From src/audit/builtin/api_surface_area.json (full template):
```json
{
  "pipeline": "api_surface_area",
  "category": "architecture",
  "description": "Detect files that export an excessive fraction of their symbols",
  "graph": [
    {"select": "symbol", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}]}},
    {"group_by": "file"},
    {"ratio": {"numerator": {"where": {"exported": true}}, "denominator": {}, "threshold": {"and": [{"count": {"gte": 10}}, {"ratio": {"gte": 0.8}}]}}},
    {"flag": {"pattern": "excessive_public_api", "message": "Excessive public API: {{file}} exports {{count}} symbols ({{ratio}}% of total)", "severity": "info"}}
  ]
}
```

From src/audit/builtin/circular_dependencies.json (full template):
```json
{
  "pipeline": "circular_dependencies",
  "category": "architecture",
  "description": "Detect circular import chains using Tarjan SCC algorithm",
  "graph": [
    {"select": "file", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}]}},
    {"find_cycles": {"edge": "imports"}},
    {"flag": {"pattern": "circular_dependency", "message": "Circular dependency ({{cycle_size}} files): {{cycle_path}}", "severity_map": [{"when": {"cycle_size": {"eq": 2}}, "severity": "info"}, {"when": {"cycle_size": {"lte": 5}}, "severity": "warning"}, {"severity": "error"}]}}
  ]
}
```

From src/audit/builtin/dependency_depth.json (full template):
```json
{
  "pipeline": "dependency_graph_depth",
  "category": "architecture",
  "description": "Detect deep import chains that complicate dependency management",
  "graph": [
    {"select": "file", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}]}},
    {"max_depth": {"edge": "imports", "skip_barrel_files": true, "threshold": {"gte": 6}}},
    {"flag": {"pattern": "deep_import_chain", "message": "Deep import chain: {{file}} is {{depth}} hops from entry points", "severity_map": [{"when": {"depth": {"gte": 13}}, "severity": "error"}, {"when": {"depth": {"gte": 9}}, "severity": "warning"}, {"severity": "info"}]}}
  ]
}
```

Language string table (from src/language.rs Language::as_str()):
- typescript -> "typescript", tsx -> "tsx", javascript -> "javascript", jsx -> "jsx"
- python -> "python", rust -> "rust", go -> "go", java -> "java"
- c -> "c", cpp -> "cpp", csharp -> "csharp", php -> "php"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 20 per-language JSON architecture pipeline files for TS/JS, Python, Rust, Go, Java</name>
  <files>
    src/audit/builtin/module_size_distribution_javascript.json
    src/audit/builtin/module_size_distribution_python.json
    src/audit/builtin/module_size_distribution_rust.json
    src/audit/builtin/module_size_distribution_go.json
    src/audit/builtin/module_size_distribution_java.json
    src/audit/builtin/circular_dependencies_javascript.json
    src/audit/builtin/circular_dependencies_python.json
    src/audit/builtin/circular_dependencies_rust.json
    src/audit/builtin/circular_dependencies_go.json
    src/audit/builtin/circular_dependencies_java.json
    src/audit/builtin/dependency_graph_depth_javascript.json
    src/audit/builtin/dependency_graph_depth_python.json
    src/audit/builtin/dependency_graph_depth_rust.json
    src/audit/builtin/dependency_graph_depth_go.json
    src/audit/builtin/dependency_graph_depth_java.json
    src/audit/builtin/api_surface_area_javascript.json
    src/audit/builtin/api_surface_area_python.json
    src/audit/builtin/api_surface_area_rust.json
    src/audit/builtin/api_surface_area_go.json
    src/audit/builtin/api_surface_area_java.json
  </files>
  <read_first>
    - src/audit/builtin/module_size_distribution.json (template)
    - src/audit/builtin/api_surface_area.json (template)
    - src/audit/builtin/circular_dependencies.json (template)
    - src/audit/builtin/dependency_depth.json (template)
    - src/language.rs (Language::as_str() values, lines 54-68)
  </read_first>
  <action>
Create 20 JSON files in `src/audit/builtin/`. Each file derives from its template with these modifications:

**Derivation rules for ALL files:**
1. Add `"languages"` field after `"description"` with language group values
2. Change `"pipeline"` field to `"{pipeline}_{lang}"` (unique per file) — e.g., `"module_size_distribution_javascript"`
3. Update `"description"` to mention the language group
4. Apply language-calibrated thresholds per the tables below
5. Keep all other graph stages identical to the template

**Language group to "languages" field mapping (per D-01):**
- JavaScript/TS: `["typescript", "javascript", "tsx", "jsx"]` — file suffix is `_javascript`
- Python: `["python"]` — file suffix is `_python`
- Rust: `["rust"]` — file suffix is `_rust`
- Go: `["go"]` — file suffix is `_go`
- Java: `["java"]` — file suffix is `_java`

**module_size_distribution_{lang}.json** — derive from `module_size_distribution.json`:
- All 5 languages: same thresholds (gte: 30 for count, severity_map unchanged)
- All 5 languages: same exclude clause (is_test_file, is_generated, is_barrel_file)
- `"pipeline"` values: `"module_size_distribution_javascript"`, `"module_size_distribution_python"`, `"module_size_distribution_rust"`, `"module_size_distribution_go"`, `"module_size_distribution_java"`

**circular_dependencies_{lang}.json** — derive from `circular_dependencies.json`:
- All 5 languages: identical to template except `"languages"` and `"pipeline"` fields
- `"pipeline"` values: `"circular_dependencies_javascript"`, `"circular_dependencies_python"`, `"circular_dependencies_rust"`, `"circular_dependencies_go"`, `"circular_dependencies_java"`

**dependency_graph_depth_{lang}.json** — derive from `dependency_depth.json` (per D-04):
- JavaScript/TS: threshold `{"gte": 6}` (same as template)
- Python: threshold `{"gte": 6}` (same as template)
- Rust: threshold `{"gte": 4}` (stricter — Rust crate structure is flatter)
- Go: threshold `{"gte": 5}` (stricter — Go packages tend shallower)
- Java: threshold `{"gte": 6}` (same as template)
- `"pipeline"` values: `"dependency_graph_depth_javascript"`, `"dependency_graph_depth_python"`, `"dependency_graph_depth_rust"`, `"dependency_graph_depth_go"`, `"dependency_graph_depth_java"`

**api_surface_area_{lang}.json** — derive from `api_surface_area.json`:
- All 5 languages: count threshold `{"gte": 10}`, ratio threshold `{"gte": 0.8}` (same as template)
- `"pipeline"` values: `"api_surface_area_javascript"`, `"api_surface_area_python"`, `"api_surface_area_rust"`, `"api_surface_area_go"`, `"api_surface_area_java"`

**Example complete file — `module_size_distribution_rust.json`:**
```json
{
  "pipeline": "module_size_distribution_rust",
  "category": "architecture",
  "description": "Detect Rust modules that have grown too large (too many symbol definitions)",
  "languages": ["rust"],
  "graph": [
    {
      "select": "symbol",
      "exclude": {
        "or": [
          {"is_test_file": true},
          {"is_generated": true},
          {"is_barrel_file": true}
        ]
      }
    },
    {"group_by": "file"},
    {"count": {"threshold": {"gte": 30}}},
    {
      "flag": {
        "pattern": "oversized_module",
        "message": "Oversized module: {{file}} has {{count}} definitions",
        "severity_map": [
          {"when": {"count": {"gte": 100}}, "severity": "error"},
          {"when": {"count": {"gte": 50}}, "severity": "warning"},
          {"severity": "info"}
        ]
      }
    }
  ]
}
```

**Example complete file — `dependency_graph_depth_rust.json`:**
```json
{
  "pipeline": "dependency_graph_depth_rust",
  "category": "architecture",
  "description": "Detect deep import chains in Rust crates",
  "languages": ["rust"],
  "graph": [
    {
      "select": "file",
      "exclude": {
        "or": [
          {"is_test_file": true},
          {"is_generated": true}
        ]
      }
    },
    {
      "max_depth": {
        "edge": "imports",
        "skip_barrel_files": true,
        "threshold": {"gte": 4}
      }
    },
    {
      "flag": {
        "pattern": "deep_import_chain",
        "message": "Deep import chain: {{file}} is {{depth}} hops from entry points",
        "severity_map": [
          {"when": {"depth": {"gte": 13}}, "severity": "error"},
          {"when": {"depth": {"gte": 9}}, "severity": "warning"},
          {"severity": "info"}
        ]
      }
    }
  ]
}
```
  </action>
  <verify>
    <automated>cd /Users/delanyoaborchie/Documents/github/virgil-cli && ls src/audit/builtin/*_javascript.json src/audit/builtin/*_python.json src/audit/builtin/*_rust.json src/audit/builtin/*_go.json src/audit/builtin/*_java.json | wc -l && cargo test --lib -- json_audit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 20 new JSON files exist in src/audit/builtin/ (4 pipelines x 5 language groups)
    - Each file has a unique "pipeline" field value ending in _javascript, _python, _rust, _go, or _java
    - Each file has a "languages" field with the correct language strings
    - Each file has "category": "architecture"
    - dependency_graph_depth_rust.json has threshold {"gte": 4}
    - dependency_graph_depth_go.json has threshold {"gte": 5}
    - dependency_graph_depth_javascript.json has threshold {"gte": 6}
    - module_size_distribution_javascript.json has "languages": ["typescript", "javascript", "tsx", "jsx"]
    - All files are valid JSON (cargo test --lib -- json_audit passes — builtin_audits() parses them all)
    - `cargo test --lib` exits 0
  </acceptance_criteria>
  <done>
    20 per-language JSON architecture pipeline files created for TypeScript/JS, Python, Rust, Go, and Java. Each file uses language-calibrated thresholds and a unique pipeline name. All JSON files parse successfully via builtin_audits().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. JSON files are embedded at compile time.

| Boundary | Description |
|----------|-------------|
| Compile-time embedding | New JSON files in src/audit/builtin/ are embedded via include_dir! — no runtime loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | T (Tampering) | JSON pipeline definitions | accept | Static JSON files compiled into binary; thresholds are non-sensitive configuration |
</threat_model>

<verification>
1. `ls src/audit/builtin/*_{javascript,python,rust,go,java}.json | wc -l` returns 20
2. `cargo test --lib -- json_audit` — all tests pass (builtin_audits parses all files)
3. `cargo test --lib` — full lib test suite passes
</verification>

<success_criteria>
- 20 new JSON files exist in src/audit/builtin/ with correct pipeline names, language filters, and calibrated thresholds
- All files are valid JSON parseable by the engine
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-fixes-architecture-json-expansion/01-02-SUMMARY.md`
</output>
