---
phase: 01-engine-fixes-architecture-json-expansion
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/audit/builtin/module_size_distribution_c.json
  - src/audit/builtin/module_size_distribution_cpp.json
  - src/audit/builtin/module_size_distribution_csharp.json
  - src/audit/builtin/module_size_distribution_php.json
  - src/audit/builtin/circular_dependencies_c.json
  - src/audit/builtin/circular_dependencies_cpp.json
  - src/audit/builtin/circular_dependencies_csharp.json
  - src/audit/builtin/circular_dependencies_php.json
  - src/audit/builtin/dependency_graph_depth_c.json
  - src/audit/builtin/dependency_graph_depth_cpp.json
  - src/audit/builtin/dependency_graph_depth_csharp.json
  - src/audit/builtin/dependency_graph_depth_php.json
  - src/audit/builtin/api_surface_area_c.json
  - src/audit/builtin/api_surface_area_cpp.json
  - src/audit/builtin/api_surface_area_csharp.json
  - src/audit/builtin/api_surface_area_php.json
autonomous: true
requirements:
  - ARCH-06
  - ARCH-07
  - ARCH-08
  - ARCH-09

must_haves:
  truths:
    - "Running virgil audit architecture against a C project returns architecture findings"
    - "Running virgil audit architecture against a C++ project returns architecture findings"
    - "Running virgil audit architecture against a C# project returns architecture findings"
    - "Running virgil audit architecture against a PHP project returns architecture findings"
  artifacts:
    - path: "src/audit/builtin/module_size_distribution_c.json"
      provides: "C module size distribution pipeline"
      contains: "module_size_distribution_c"
    - path: "src/audit/builtin/module_size_distribution_cpp.json"
      provides: "C++ module size distribution pipeline"
      contains: "module_size_distribution_cpp"
    - path: "src/audit/builtin/module_size_distribution_csharp.json"
      provides: "C# module size distribution pipeline"
      contains: "module_size_distribution_csharp"
    - path: "src/audit/builtin/module_size_distribution_php.json"
      provides: "PHP module size distribution pipeline"
      contains: "module_size_distribution_php"
  key_links:
    - from: "src/audit/builtin/*.json"
      to: "src/audit/json_audit.rs"
      via: "include_dir! compile-time embedding"
      pattern: "BUILTIN_AUDITS_DIR"
---

<objective>
Create 16 per-language JSON architecture pipeline files for the remaining 4 language groups (C, C++, C#, PHP) — 4 pipelines per language group. Each file uses language-calibrated thresholds and includes the "languages" filter field. PHP gets a raised api_surface_area threshold (15 symbols vs 10) per D-04.

Purpose: Complete architecture audit coverage for all 9 language groups. Runs in parallel with Plan 02 (different files, no overlap).
Output: 16 new JSON files in `src/audit/builtin/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-fixes-architecture-json-expansion/01-RESEARCH.md
@.planning/phases/01-engine-fixes-architecture-json-expansion/01-PATTERNS.md
@.planning/phases/01-engine-fixes-architecture-json-expansion/01-01-SUMMARY.md

<interfaces>
<!-- Templates to derive from — read these before writing any JSON files -->

From src/audit/builtin/module_size_distribution.json:
```json
{
  "pipeline": "module_size_distribution",
  "category": "architecture",
  "description": "Detect modules that have grown too large (too many symbol definitions)",
  "graph": [
    {"select": "symbol", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}, {"is_barrel_file": true}]}},
    {"group_by": "file"},
    {"count": {"threshold": {"gte": 30}}},
    {"flag": {"pattern": "oversized_module", "message": "Oversized module: {{file}} has {{count}} definitions", "severity_map": [{"when": {"count": {"gte": 100}}, "severity": "error"}, {"when": {"count": {"gte": 50}}, "severity": "warning"}, {"severity": "info"}]}}
  ]
}
```

From src/audit/builtin/api_surface_area.json:
```json
{
  "pipeline": "api_surface_area",
  "category": "architecture",
  "description": "Detect files that export an excessive fraction of their symbols",
  "graph": [
    {"select": "symbol", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}]}},
    {"group_by": "file"},
    {"ratio": {"numerator": {"where": {"exported": true}}, "denominator": {}, "threshold": {"and": [{"count": {"gte": 10}}, {"ratio": {"gte": 0.8}}]}}},
    {"flag": {"pattern": "excessive_public_api", "message": "Excessive public API: {{file}} exports {{count}} symbols ({{ratio}}% of total)", "severity": "info"}}
  ]
}
```

From src/audit/builtin/circular_dependencies.json:
```json
{
  "pipeline": "circular_dependencies",
  "category": "architecture",
  "description": "Detect circular import chains using Tarjan SCC algorithm",
  "graph": [
    {"select": "file", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}]}},
    {"find_cycles": {"edge": "imports"}},
    {"flag": {"pattern": "circular_dependency", "message": "Circular dependency ({{cycle_size}} files): {{cycle_path}}", "severity_map": [{"when": {"cycle_size": {"eq": 2}}, "severity": "info"}, {"when": {"cycle_size": {"lte": 5}}, "severity": "warning"}, {"severity": "error"}]}}
  ]
}
```

From src/audit/builtin/dependency_depth.json:
```json
{
  "pipeline": "dependency_graph_depth",
  "category": "architecture",
  "description": "Detect deep import chains that complicate dependency management",
  "graph": [
    {"select": "file", "exclude": {"or": [{"is_test_file": true}, {"is_generated": true}]}},
    {"max_depth": {"edge": "imports", "skip_barrel_files": true, "threshold": {"gte": 6}}},
    {"flag": {"pattern": "deep_import_chain", "message": "Deep import chain: {{file}} is {{depth}} hops from entry points", "severity_map": [{"when": {"depth": {"gte": 13}}, "severity": "error"}, {"when": {"depth": {"gte": 9}}, "severity": "warning"}, {"severity": "info"}]}}
  ]
}
```

Language string table (from src/language.rs):
- c -> "c", cpp -> "cpp", csharp -> "csharp", php -> "php"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 16 per-language JSON architecture pipeline files for C, C++, C#, PHP</name>
  <files>
    src/audit/builtin/module_size_distribution_c.json
    src/audit/builtin/module_size_distribution_cpp.json
    src/audit/builtin/module_size_distribution_csharp.json
    src/audit/builtin/module_size_distribution_php.json
    src/audit/builtin/circular_dependencies_c.json
    src/audit/builtin/circular_dependencies_cpp.json
    src/audit/builtin/circular_dependencies_csharp.json
    src/audit/builtin/circular_dependencies_php.json
    src/audit/builtin/dependency_graph_depth_c.json
    src/audit/builtin/dependency_graph_depth_cpp.json
    src/audit/builtin/dependency_graph_depth_csharp.json
    src/audit/builtin/dependency_graph_depth_php.json
    src/audit/builtin/api_surface_area_c.json
    src/audit/builtin/api_surface_area_cpp.json
    src/audit/builtin/api_surface_area_csharp.json
    src/audit/builtin/api_surface_area_php.json
  </files>
  <read_first>
    - src/audit/builtin/module_size_distribution.json (template)
    - src/audit/builtin/api_surface_area.json (template)
    - src/audit/builtin/circular_dependencies.json (template)
    - src/audit/builtin/dependency_depth.json (template)
    - src/language.rs (Language::as_str() values, lines 54-68)
  </read_first>
  <action>
Create 16 JSON files in `src/audit/builtin/`. Each file derives from its template with these modifications:

**Language group to "languages" field mapping:**
- C: `["c"]` — file suffix is `_c`
- C++: `["cpp"]` — file suffix is `_cpp`
- C#: `["csharp"]` — file suffix is `_csharp`
- PHP: `["php"]` — file suffix is `_php`

**module_size_distribution_{lang}.json** — derive from `module_size_distribution.json`:
- All 4 languages: same thresholds (gte: 30 for count, severity_map unchanged)
- All 4 languages: same exclude clause (is_test_file, is_generated, is_barrel_file)
- `"pipeline"` values: `"module_size_distribution_c"`, `"module_size_distribution_cpp"`, `"module_size_distribution_csharp"`, `"module_size_distribution_php"`
- Known limitation for C/C++: `.h` header files will be flagged as oversized. The DSL has no extension-based exclusion. This is documented and accepted for Phase 1.

**circular_dependencies_{lang}.json** — derive from `circular_dependencies.json`:
- All 4 languages: identical to template except `"languages"` and `"pipeline"` fields
- `"pipeline"` values: `"circular_dependencies_c"`, `"circular_dependencies_cpp"`, `"circular_dependencies_csharp"`, `"circular_dependencies_php"`

**dependency_graph_depth_{lang}.json** — derive from `dependency_depth.json` (per D-04):
- C: threshold `{"gte": 4}` (header inclusion chains should be shallow)
- C++: threshold `{"gte": 4}` (same as C)
- C#: threshold `{"gte": 6}` (assembly nesting common)
- PHP: threshold `{"gte": 6}` (Composer dependency depth can be deep)
- `"pipeline"` values: `"dependency_graph_depth_c"`, `"dependency_graph_depth_cpp"`, `"dependency_graph_depth_csharp"`, `"dependency_graph_depth_php"`

**api_surface_area_{lang}.json** — derive from `api_surface_area.json`:
- C, C++, C#: count threshold `{"gte": 10}`, ratio `{"gte": 0.8}` (same as template)
- **PHP (per D-04):** count threshold `{"gte": 15}` (raised from 10 because PHP exports all top-level declarations by default), ratio `{"gte": 0.8}`
- `"pipeline"` values: `"api_surface_area_c"`, `"api_surface_area_cpp"`, `"api_surface_area_csharp"`, `"api_surface_area_php"`

**Example complete file — `api_surface_area_php.json` (note raised count threshold):**
```json
{
  "pipeline": "api_surface_area_php",
  "category": "architecture",
  "description": "Detect PHP files that export an excessive fraction of their symbols",
  "languages": ["php"],
  "graph": [
    {
      "select": "symbol",
      "exclude": {
        "or": [
          {"is_test_file": true},
          {"is_generated": true}
        ]
      }
    },
    {"group_by": "file"},
    {
      "ratio": {
        "numerator": {"where": {"exported": true}},
        "denominator": {},
        "threshold": {
          "and": [
            {"count": {"gte": 15}},
            {"ratio": {"gte": 0.8}}
          ]
        }
      }
    },
    {
      "flag": {
        "pattern": "excessive_public_api",
        "message": "Excessive public API: {{file}} exports {{count}} symbols ({{ratio}}% of total)",
        "severity": "info"
      }
    }
  ]
}
```

**Example complete file — `dependency_graph_depth_c.json` (note gte: 4):**
```json
{
  "pipeline": "dependency_graph_depth_c",
  "category": "architecture",
  "description": "Detect deep include chains in C codebases",
  "languages": ["c"],
  "graph": [
    {
      "select": "file",
      "exclude": {
        "or": [
          {"is_test_file": true},
          {"is_generated": true}
        ]
      }
    },
    {
      "max_depth": {
        "edge": "imports",
        "skip_barrel_files": true,
        "threshold": {"gte": 4}
      }
    },
    {
      "flag": {
        "pattern": "deep_import_chain",
        "message": "Deep import chain: {{file}} is {{depth}} hops from entry points",
        "severity_map": [
          {"when": {"depth": {"gte": 13}}, "severity": "error"},
          {"when": {"depth": {"gte": 9}}, "severity": "warning"},
          {"severity": "info"}
        ]
      }
    }
  ]
}
```
  </action>
  <verify>
    <automated>cd /Users/delanyoaborchie/Documents/github/virgil-cli && ls src/audit/builtin/*_c.json src/audit/builtin/*_cpp.json src/audit/builtin/*_csharp.json src/audit/builtin/*_php.json | wc -l && cargo test --lib -- json_audit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 16 new JSON files exist in src/audit/builtin/ (4 pipelines x 4 language groups)
    - Each file has a unique "pipeline" field value ending in _c, _cpp, _csharp, or _php
    - Each file has a "languages" field with the correct language string
    - Each file has "category": "architecture"
    - dependency_graph_depth_c.json has threshold {"gte": 4}
    - dependency_graph_depth_cpp.json has threshold {"gte": 4}
    - dependency_graph_depth_csharp.json has threshold {"gte": 6}
    - dependency_graph_depth_php.json has threshold {"gte": 6}
    - api_surface_area_php.json has count threshold {"gte": 15} (not 10)
    - api_surface_area_c.json has count threshold {"gte": 10}
    - All files are valid JSON (cargo test --lib -- json_audit passes)
    - `cargo test --lib` exits 0
  </acceptance_criteria>
  <done>
    16 per-language JSON architecture pipeline files created for C, C++, C#, and PHP. PHP has raised api_surface_area threshold. C/C++ have stricter depth thresholds. All JSON files parse successfully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. JSON files are embedded at compile time.

| Boundary | Description |
|----------|-------------|
| Compile-time embedding | New JSON files in src/audit/builtin/ are embedded via include_dir! — no runtime loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T (Tampering) | JSON pipeline definitions | accept | Static JSON files compiled into binary; thresholds are non-sensitive configuration |
</threat_model>

<verification>
1. `ls src/audit/builtin/*_{c,cpp,csharp,php}.json | wc -l` returns 16
2. `cargo test --lib -- json_audit` — all tests pass
3. `cargo test --lib` — full lib test suite passes
</verification>

<success_criteria>
- 16 new JSON files exist in src/audit/builtin/ with correct pipeline names, language filters, and calibrated thresholds
- PHP api_surface_area has raised threshold (15 symbols)
- C/C++ depth threshold is 4 (stricter)
- All files are valid JSON parseable by the engine
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-fixes-architecture-json-expansion/01-03-SUMMARY.md`
</output>
